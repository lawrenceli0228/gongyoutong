# W4 · 跨平台运行方案(最终版)

> 2026-08-09 · `/plan-eng-review` 二轮产出 · 基准 `origin/main@8464ab0` + `lawrence/demo-polish@64d8ef3`
> **本版取代 8-09 上午那版。** 变更两处:① 从「演示统一/开发各随其便」改为**全面统一 Docker**;
> ② 统一 `data_dir` 之后,路径方案从「加配置项」缩成「改一个默认值」,diff 更小。
> 新输入:队友的《本地启动手册_Mac.md》——它暴露了两个此前没记录的问题。

---

## 0. 结论

**两人都用 Docker 启动,代码仍在本机编辑(挂源码热重载)。**

这不是新建架构。项目在 T1 就建好了四阶段 Dockerfile(BGE-M3 权重预烤 + `HF_HUB_OFFLINE=1` 断网可用)
和 docker-compose。挡路的是**五处路径与依赖问题**,修完就通。

**VPS 不做**(§7)。它解决的是另一类问题,而且本方案是它的前置。

### 为什么统一 Docker 对队友是净收益,不是牺牲

| | 他现在的本机路径 | 统一 Docker 之后 |
|---|---|---|
| 装依赖 | sentence-transformers + torch ≈ 几 GB | 镜像里已装好 |
| BGE-M3 权重 | 本机下 2.2GB | 镜像层已烤好 |
| 建知识库 | 本机跑 15 分钟 | 容器里跑,一次 |
| 改代码 | 编辑器直接改,热重载 | **一样**(dev override 挂源码) |
| 启动命令 | `make dev` | `make dev-docker` |

他付出的只有「换一条启动命令 + 首次构建等一次」,换来的是不用在本机维护一套重依赖。

---

## 1. 现有资产:别造已经存在的东西

```
backend/Dockerfile(四阶段,T1 就写好了)
  uvbin ──► base ──► deps        (uv sync 装第三方库)
                └──► models      (snapshot_download BAAI/bge-m3,约 2.2GB 烤进镜像层)
                        └──► app (非 root / EXPOSE 2024 / langgraph dev --allow-blocking)

运行期 HF_HUB_OFFLINE=1  ──►  冷启动零下载,演示现场断网也起得来
docker-compose.yml       ──►  backend + frontend(--profile ui)+ healthcheck + 127.0.0.1 绑定
backend/README.md        ──►  已有完整 WSL2 指引
队友的《本地启动手册_Mac.md》──► 可复用,但需两处修正(见 §6)
```

**一个巧合值得说:** 队友的 knowledge Agent 用的正是这个预烤好的 BGE-M3。
T1 时为「冷启动零下载」做的决策,在两周后恰好接住了 W3 才落地的 RAG。

### 你的 Intel Mac 在这条路上是优势

| 平台 | 容器架构 | torch 2.13.0 轮子 |
|---|---|---|
| **你的 Mac(Intel x86_64)** | **原生 linux/amd64,无翻译层** | 容器内 manylinux_x86_64 ✓ |
| 队友 Windows(WSL2) | 原生 linux/amd64 | 同上 ✓ |
| (对照)M 系列 Mac | 要 QEMU 模拟,慢 | — |
| (对照)本机原生装 | — | **macOS x86_64 轮子不存在 ✗** |

torch 那条 BLOCKER 在容器里根本不存在。换 M 系列 Mac 这条路反而更慢。

---

## 2. 目标架构

```
   Mac (Intel)                                         Windows + WSL2
   ┌──────────┐        ┌─────────────────────────┐      ┌──────────┐
   │ 编辑器   │───挂源码►│ gyt-backend  :2024      │◄─挂源码──│ 编辑器   │
   │ 改代码   │        │  5 Agent 全可用          │      │ 改代码   │
   └──────────┘        │  BGE-M3 已烤进镜像       │      └──────────┘
                       └───────────┬─────────────┘
                                   │ 挂载 ./data:/app/data
                       ┌───────────▼─────────────┐
                       │ <repo>/data/            │  ← 统一到这一个目录(D2)
                       │   demo/      源资产(进git)│
                       │   artifacts/ 巡检记录     │
                       │   chroma/    规范向量库   │
                       │   cache/     视觉缓存     │  ← 焐热一次,处处命中
                       │   gyt.sqlite3 任务台账    │
                       └─────────────────────────┘

  docker-compose.yml          交付/演示态 —— 纯镜像,不挂源码
  + docker-compose.dev.yml    开发态 —— 追加挂 ./backend/src,热重载

  本机 backend/data/ 基本退役(只剩 make test 用 tmp_path,不碰它)
```

**为什么分两份 compose:** 演示要的是「镜像里是什么就跑什么」。挂源码会让镜像与实际运行的代码
不一致,出问题时分不清是镜像坏了还是本机代码坏了。两种用途,两份配置。

---

## 3. 已定的决策

| # | 决策 | 选择 | 理由 |
|---|---|---|---|
| D1 | 跨平台策略 | **全面统一 Docker 启动** | 一套环境;队友净收益(不用本机维护几 GB 重依赖) |
| D2 | 数据目录 | **统一到仓库根 `data/`** | 缓存/台账/知识库/产物四样共享;**顺带让 D4 从「加配置项」缩成「改默认值」** |
| D3 | 建库信号 | **分开退出码 + 明确原因** | 「没找到 PDF」和「已入库」现在是同一句话+同一个退出码,是所有路径 bug 的放大器 |
| D4 | 资产路径 | **随 D2 自动解决** | 统一后 demo 资产就是 `data_dir/"demo"`,本机与容器自动对齐 |
| D5 | 镜像体积 | **CPU-only torch** | 砍 2.4GB CUDA(代码已 `device="cpu"`),镜像/CI/首次构建三处提速 |
| D6 | 开发回路 | **dev override 挂源码** | 否则改一行要重建;这也是队友肯换过来的前提 |
| D7 | 验收覆盖 | **补 E/F 两组 + 路径铁证** | 路径 bug 的失败方式是静默的,必须有断言而不是手点 |

---

## 4. 实施

### 阶段一 · 依赖与体积(D5)

`backend/pyproject.toml` 追加:

```toml
[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux'" }]
```

```bash
cd backend && uv lock && uv lock --check
```

> ⚠️ `uv.lock` 是队友刚提交的。动之前跟他说一声,别两边同时改。
> ⚠️ 若 `sentence-transformers` 对 torch 有版本下限冲突,**实测报错再调版本号,不要凭记忆填**。

### 阶段二 · 统一数据目录(D2 + D4)

**`backend/src/gyt/config.py`** —— 只改一个默认值:

```python
def _default_data_dir() -> Path:
    """数据根目录的默认值。

    本文件是 <repo>/backend/src/gyt/config.py:
        parents[0]=gyt  [1]=src  [2]=backend  [3]=<仓库根>

    ⚠️ 原来是 Path("data"),按**进程工作目录**解析 —— 而 make dev 在 backend/ 下跑,
       数据落在 backend/data;容器里 GYT_DATA_DIR=/app/data(compose 挂的是仓库根 ./data)。
       两条路径因此有两份数据:缓存、台账、知识库、产物全部分裂。
       统一到仓库根之后,demo 资产就是 data_dir/"demo",本机与容器自动对齐,
       不需要再为演示资产单开一个配置项。
    """
    return Path(__file__).resolve().parents[3] / "data"


data_dir: Path = Field(default_factory=_default_data_dir)
```

同一个文件加一个派生属性(**不要 `_ensure_dir`** —— 它是源资产,自动创建反而会掩盖「资产丢了」):

```python
@property
def demo_assets_dir(self) -> Path:
    """演示源资产(照片/图纸/规范 PDF)。随仓库走、进 git。

    与运行期数据同在 data_dir 下:容器 /app/data/demo,本机 <repo>/data/demo —— 自动对齐。
    刻意不建目录:目录不存在本身就是要报出来的错(见 ingest 的退出码)。
    """
    return self.data_dir / "demo"
```

**`agents/knowledge/ingest.py:40`** 与 **`agents/cad/demo_registry.py:37`** 改成读配置。
别在模块级调 `get_settings()`(import 时固化,测试改环境变量就失效):

```python
# 之前:_REPO_ROOT = Path(__file__).resolve().parents[5]
#       DEFAULT_DOCS_DIR = _REPO_ROOT / "data" / "demo" / "docs"
def _docs_dir() -> Path:
    return get_settings().demo_assets_dir / "docs"
```

两处文件顶部那句「`parents[5]` = 仓库根」的注释一并改掉。**陈旧注释比没注释更害人**,这是 CLAUDE.md 里的规矩。

**一次性搬迁**(本机现有数据):

```bash
cd <repo>
mkdir -p data
mv backend/data/* data/ 2>/dev/null || true    # demo/ 已在 data/ 下,不冲突
rmdir backend/data 2>/dev/null || true
```

> 视觉缓存搬过去照样命中 —— 缓存键算在**图片内容**上,不含路径。台账 sqlite 同理。
> `.gitignore` 不用改:现有的 `/data/*` + `!/data/demo/` 正好就是对的规则。

### 阶段三 · 建库信号(D3)

`agents/knowledge/ingest.py` 的 `main()`,把两种失败从 `build_index` 提上来并给 exit 1:

```python
def main() -> int:
    docs_dir = _docs_dir()
    if not docs_dir.is_dir():
        print(f"[错误] 规范目录不存在:{docs_dir}")
        print("       容器里确认 compose 挂了 ./data:/app/data;本机确认已把 backend/data 搬到仓库根 data/。")
        return 1
    pdfs = sorted(docs_dir.glob("*.pdf"))
    if not pdfs:
        print(f"[错误] {docs_dir} 里一份 PDF 都没有 —— 建出来的知识库会是空的。")
        return 1

    result = build_index(rebuild="--rebuild" in sys.argv)
    if not result:
        print(f"全部已入库、无需重建(共 {len(pdfs)} 份规范)。")   # ← 这才是真的成功
        return 0
    print("已建库:")
    ...
```

**为什么这条优先级高:** 它是所有路径类 bug 的**放大器**。修完之后,D2 的搬迁、
容器挂载写错、队友换机器——任何一种路径问题都会当场报错,而不是等到演示时
knowledge 说「规范里查不到」(而那句话跟真查不到长得一模一样)。

### 阶段四 · 开发回路(D6)

新增 **`docker-compose.dev.yml`**:

```yaml
# 开发态叠加层。用法(仓库根):
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
# 或 make dev-docker
#
# 为什么单独一份:演示要「镜像里是什么就跑什么」。挂源码会让镜像与实际运行的代码
# 不一致,出问题时分不清是镜像坏了还是本机代码坏了。两种用途,两份配置。
services:
  backend:
    volumes:
      - ./backend/src:/app/src:ro
```

`Makefile`:

```make
COMPOSE_DEV    := -f docker-compose.yml -f docker-compose.dev.yml

dev-docker: ## 容器内起后端并挂本机源码(热重载;日常开发用这个)
	$(COMPOSE) $(COMPOSE_DEV) up --build
```

### 阶段五 · 收尾(无决策空间)

- `ARTIFACTS_DIR ?= $(DATA_DIR)/artifacts` —— D2 统一后本来就对齐了,只需去掉 `backend/` 前缀
- `.PHONY` 补 `build-knowledge`(合并残留)
- `TOOL_NAMES` 补队友那 7 个工具名:`list_drawings` / `parse_drawing` / `query_dimension` /
  `list_components` / `layer_stats` / `render_preview` / `search_regulation`,否则界面显示英文函数名

### 阶段六 · 真机验收扩容(D7)

`backend/scripts/live_acceptance.py` 加两组:

```
E 组 · cad(3 条)
  E1  「有哪些图纸」          → 派给 cad,且返回图纸数 > 0    ← 路径铁证
  E2  「首层平面图有哪些图层」→ 列出图层,不报「一张图都没有」
  E3  库级:cad_index/ 下 json 数 == 演示图纸数

F 组 · knowledge(3 条)
  F1  「消防车道最小净宽是多少」→ 派给 knowledge
  F2  回答必须带《文件名》+ 页码(不带 = 判失败,它是 RAG 的全部价值)
  F3  库级:chroma 里文档数 > 0                              ← 路径铁证
```

> F1 的问法直接取自队友手册的测试用例,期望答「不小于 4 米」+ GB50016 页码。
> **E3/F3 是重点:** 路径错了这两条当场变红,而不是让系统静默地说「查不到」。

---

## 5. 日常怎么用

```bash
# 一次性
cp .env.example .env          # 填两把 Key
docker compose -f docker-compose.yml -f docker-compose.dev.yml build   # 首次 10~30 分钟

# 建知识库(容器里,一次)
docker compose run --rm backend python -m gyt.agents.knowledge.ingest
#   ↑ 修完 D3 之后,路径不对会当场 exit 1 报错,而不是假装成功

# 日常开发
make dev-docker               # 起后端 :2024,挂源码热重载
cd frontend && pnpm dev       # 前端 :3000(前端仍在本机跑,它没有 torch 问题)

# 收摊
make down
```

**前端为什么不进容器:** 它没有跨平台问题(Node 到处都能装),而 `pnpm dev` 的热重载
比容器里顺。compose 的 `--profile ui` 留着给演示日用。

---

## 6. 队友手册要改的地方

他那份《本地启动手册_Mac.md》基本可以复用,但有**两处必须改**、一处建议补:

| 位置 | 现状 | 改成 |
|---|---|---|
| **第 88 行** | 「看到 `已建库` 或 **`没有新增入库`** 就成了」 | **只有 `已建库` 或 `全部已入库` 才算成功;看到 `[错误]` 开头就是失败** |
| **第 86 行** | 「落到 `backend/data/chroma`」 | `<repo>/data/chroma`(D2 统一后) |
| 第 2 步 | `make setup` 装本机依赖 | 补一句:**Intel Mac 装不上 torch,走 Docker 路径** |

> 第 88 行是最要紧的:它把**失败信号写成了成功判据**。修完 D3 之后这句话会真的成立,
> 但在此之前照它做会得到一个空知识库而不自知。

**另外给他的三条**(队友泳道,不在本方案范围内,单独告知):

1. `list_components` **谎报构件所在图层** —— `layers_with` 取的是全图所有含 INSERT 的图层,
   与被查的那个块无关,却拼成「都在「X」层」。演示时会当着评委的面说错话。
2. **库空与真查不到用同一句话** —— `search_regulation` 建议在检索前判库是否为空,
   空则返回不同的 `user_msg`(「知识库还没建」)。
3. **`ruff format --check` 有 6 个文件不合格**,全来自新代码,**CI 会红**(`ci.yml:182` 确实跑这条)。
   `ruff format .` 一条命令全修好。

---

## 7. VPS:能解决,但不该做主路径

| 场景 | VPS 是否解决 |
|---|---|
| Intel Mac 装不上 torch | ✅(Docker 也解决,且不花钱) |
| 两人环境不一致 | ✅ 最彻底 |
| 队友首次构建等 30 分钟 | ✅ |
| 评委扫码自己体验 | ✅ **只有 VPS 能做**(TODO-1 的原始动机) |
| 演示机故障兜底 | ✅ 第四层 |
| **演示现场主路径** | ❌ **不该用** |

**为什么不做主路径:**

- 项目 **D9 决策**是「本地主跑 + 三层兜底」。把主路径押在会场 WiFi 上与既定决策冲突。
- **视觉缓存跟着机器走** —— 本机焐热的 22 分钟,VPS 上不认。

**硬前置 = TODO-7(鉴权):** `:2024` 是**零鉴权**的 Agent 执行端点,而两把 API Key 就在容器里。
`docker-compose.yml` 钉死 `127.0.0.1` 正是为了挡这个。**公网裸奔 = 账单敞开给人刷。**

最小安全集:① `backend/auth.py` + `langgraph.json` 声明 auth;② Caddy 反代 + 访问口令;
③ `/runs/*` 限流(`recursion_limit` 只限单次 run 的步数,不限次数);④ 2C4G 起,BGE-M3 吃内存。

**建议:现在别做。** 本方案做完后 VPS 只是「把同一份 compose 跑在别人机器上」,约 1 小时;
反过来不成立。已写进 TODO-1。

---

## 8. NOT in scope

| 项 | 为什么不做 |
|---|---|
| VPS 部署 | §7。演示后再说,且本方案是它的前置 |
| LangGraph 鉴权(TODO-7) | 只在公网暴露时必需;本地回环绑定已挡住 |
| 改 Docker 构建上下文到仓库根 | 会破坏四阶段分层缓存,重建 = 重烤 2.2GB 权重 |
| RAG 依赖做成 optional-extra | 统一 Docker 后容器里本来就装得上,extras 只增加「装没装全」这个维度 |
| 本机原生跑 knowledge | Intel Mac 无 torch 轮子,上游事实 |
| 前端也进容器 | 它没有跨平台问题,`pnpm dev` 热重载更顺 |
| `list_components` 谎报图层 | 真 bug,但属队友泳道功能缺陷,单独告知(§6) |
| 人脸打码 / 素材合规(TODO-22) | 演示前必做,但与跨平台无关 |

---

## 9. 失败模式

| 失败 | 测试 | 用户看到 | 判定 |
|---|---|---|---|
| 容器里 demo 资产路径错 | **E3/F3 + D3 的 exit 1** | 当场报错 | ✅ 已封 |
| 建库时目录不存在 | **D3 的 exit 1** | `[错误] 规范目录不存在: /xxx` | ✅ 已封 |
| knowledge 库空但目录在 | F3 断言库非空 | 「规范里没这条」 | ⚠️ **仍静默** → 见下 |
| `make up` 不带 `--build` | 无 | 功能正常但少 Agent | ⚠️ 靠 checklist |
| torch 版本冲突 | `uv lock --check` | 构建失败,明确 | ✅ 可接受 |
| 挂源码后 uid 不一致 | 无 | Linux 上权限拒绝 | ✅ Docker Desktop 自动处理 |
| BGE-M3 首次加载 2.2GB | 无超时 | 第一次问规范卡几十秒 | ⚠️ 靠预热 |
| 搬迁后忘了旧数据 | 无 | 台账/缓存"消失" | ⚠️ 其实是换了目录,checklist 提醒 |

**三个 ⚠️ 的处理:**

1. **库空 vs 真查不到** —— 本方案没解决(属队友泳道)。已列入 §6 告知清单。
2. **旧镜像** —— `make dev-docker` 已带 `--build`;`make up` 那条要在 checklist 里钉死。
3. **BGE-M3 冷启动** —— 队友在 Makefile 注释里明确警告**不许**启动时建库(15 分钟会拖垮
   healthcheck 的 120s `start_period`,容器反复重启永不 healthy)。只能演示前手动问一次规范预热。

---

## 10. 演示日 checklist(因本方案新增)

```
[ ] docker compose build 至少跑过一次,确认新依赖解析成功
[ ] 数据已搬到仓库根 data/,backend/data 已空(否则缓存/台账像"消失了")
[ ] 容器内建库:docker compose run --rm backend python -m gyt.agents.knowledge.ingest
    → 必须看到「已建库」或「全部已入库」;看到 [错误] 就是路径没对
[ ] 容器内验证资产:docker compose exec backend ls /app/data/demo/{docs,drawings}
[ ] 手动问一次规范,把 BGE-M3 加载进内存(否则演示第一问卡几十秒)
[ ] 手动问一次图纸,确认 cad 能列出图纸
[ ] make up 带 --build(否则跑的是旧镜像,本机那份是 3 天前只有 ping agent 的)
[ ] live_acceptance.py 跑到 25/25(含新增 E/F 组)
```

⚠️ **既有陷阱提醒:** 19 条断言的日期锚死在 2026-08-08(8月9日周日/8月14日周五/8月10日周一)。
演示日不是这天的话 A1/A3/A4/C3 会红,**那是断言过期不是系统坏了** —— 彩排前先按当天重算。

---

## 11. 并行化

```
Lane A(依赖):  T1  CPU-only torch → uv lock → 构建验证
Lane B(数据):  T2  data_dir 统一 → 搬迁 → demo_assets_dir 派生属性 → 两个 agent 改读配置
Lane C(信号):  T3  ingest 退出码            ← 依赖 B 的 _docs_dir()
Lane D(回路):  T4  dev override + Makefile
Lane E(收尾):  T5  ARTIFACTS_DIR / .PHONY / TOOL_NAMES

  ⚠️ D 与 E 都碰 Makefile → 合并成一条 lane 顺序做
  T6(验收断言)依赖 B + C

执行:A ∥ B ∥ (D→E) 并行 → B 完成后接 C → 合并 → T6 → 彩排
```

---

## 12. 实施任务

- [ ] **T1 (P1, human ~1h / CC ~20min)** — 依赖 — torch 换 CPU-only 索引并重新 lock
  - 由来:D5;Linux 轮子 3.31GB 里 2.4GB 是永不加载的 CUDA
  - 文件:`backend/pyproject.toml`、`backend/uv.lock`
  - 验证:`uv lock --check` + 镜像 < 5GB
- [ ] **T2 (P1, human ~1.5h / CC ~25min)** — 配置 — `data_dir` 统一到仓库根 + 搬迁 + 两个 agent 改读配置
  - 由来:D2/D4;两条路径两份数据,缓存/台账/知识库/产物全分裂
  - 文件:`config.py`、`knowledge/ingest.py`、`cad/demo_registry.py`、(搬迁 `backend/data/*`)
  - 验证:`docker compose exec backend python -c "from gyt.config import get_settings as g; print(g().demo_assets_dir)"` → `/app/data/demo`
- [ ] **T3 (P1, human ~1h / CC ~15min)** — 建库 — ingest 分开退出码 + 明确原因
  - 由来:D3;「没找到 PDF」和「已入库」压成同一句话+同一个退出码
  - 文件:`agents/knowledge/ingest.py`
  - 验证:故意把 `GYT_DATA_DIR` 指到空目录,必须 exit 1 且打印具体路径
- [ ] **T4 (P1, human ~1h / CC ~15min)** — 回路 — dev override + `make dev-docker`
  - 由来:D6;compose 只挂 data 不挂源码,改一行要重建
  - 文件:`docker-compose.dev.yml`(新)、`Makefile`
  - 验证:起容器后改 `prompt.md`,不重建即生效
- [ ] **T5 (P2, human ~20min / CC ~5min)** — 收尾 — `ARTIFACTS_DIR` / `.PHONY` / `TOOL_NAMES`
  - 由来:合并残留 + 审查发现
  - 文件:`Makefile`、`scripts/frontend-overrides/tool-calls.tsx`
  - 验证:界面展开痕迹显示中文工具名
- [ ] **T6 (P1, human ~3h / CC ~30min)** — 验收 — live_acceptance 补 E/F 两组
  - 由来:D7;19 断言只覆盖 5 个 Agent 里的 3 个
  - 文件:`backend/scripts/live_acceptance.py`
  - 验证:25/25;故意把资产目录挪走,E3/F3 必须变红
- [ ] **T7 (P1, human ~30min / CC ~10min)** — 协作 — 给队友的手册修正 + 三条 bug
  - 由来:队友手册第 88/86 行 + 12-agent 审查
  - 文件:(沟通,不改他的文件)
  - 验证:队友确认收到并回复
- [ ] **T8 (P2, human ~10min / CC ~3min)** — 文档 — `backend/README.md` 补统一 Docker 的启动段
  - 由来:D1;README 现在教的是本机路径
  - 文件:`backend/README.md`
  - 验证:新人照 README 能起来

---

## 13. 完成状态

- Step 0:**scope 未扩大** — 确认为「修五处路径与依赖问题」,非新建架构
- Architecture:3 issues(D1 统一程度 / D2 数据目录 / D5 镜像体积)
- Code Quality:2 issues(D3 建库信号 / D4 随 D2 自动解决)
- Test:覆盖 3/5 Agent → 补 E/F 两组,6 条新断言(含 2 条路径铁证)
- Performance:1 finding(BGE-M3 冷启动),解法已被队友注释锁定 → 转 checklist
- 失败模式:**3 个 ⚠️**,0 个 critical gap
- 并行化:5 lanes,3 并行 + 2 串行
- **相对上一版:diff 更小**(D2 让 D4 从「加配置项」缩成「改默认值」)
