# TODOS — 工友通 AI Agent

> 由 /plan-eng-review 于 2026-08-05 评审时与负责人逐条确认后记录。
> 每条含完整上下文,三个月后捡起可直接开工。

## TODO-1 云部署 + 访问口令(W4 加分项)
- **What:** docker-compose 整栈部署到轻量云服务器(2C4G 起),Caddy 反代 + 全局访问口令中间件,公网可访问。
- **Why:** 评委扫码自己体验,超出「看演示」的印象分;同时是演示机故障时的第四层兜底。
- **Pros:** 传播性与印象分;异地队友/导师可随时看进度。
- **Cons:** 约 1 天运维;公网暴面需口令保护(D10 已定)防 API 账单被刷。
- **Context:** D9 决定「本地主跑+三层兜底」,云是 W4 有余力才做的加分项,不是主路。起步:租云→compose up→配口令。
- **Depends:** W3 末主线功能冻结后。
- **⚠️ 补充(2026-08-09,`/plan-eng-review` 定,详见 `docs/W4_跨平台运行方案.md` §6):**
  1. **前置是 W4 跨平台方案,不是反过来。** 那个方案把「统一跑 Docker」这条路修通(资产路径收进
     config、CPU-only torch、dev override)。**做完之后 VPS 只是「把同一份 compose 跑在别人机器上」,
     约 1 小时;反过来不成立。** 排期时别把 VPS 排在跨平台方案之前。
  2. **硬前置 = TODO-7(鉴权),这是安全红线不是优化项。** `:2024` 是**零鉴权**的 Agent 执行端点,
     而 DeepSeek / Kimi 的 API Key 就在容器里。`docker-compose.yml` 现在钉死 `127.0.0.1:2024:2024`
     正是为了挡住这个 —— **公网裸奔 = 账单敞开给人刷**。
  3. **安全最小集(四条,少一条都别上公网):**
     ① `backend/auth.py` + `langgraph.json` 声明 `"auth": {"path": "./auth.py:auth"}`;
     ② Caddy 反代 + 全局访问口令;
     ③ `/runs/*` 简单限流(`supervisor_recursion_limit` 只限单次 run 的**步数**,不限 run 的**次数**);
     ④ 规格 2C4G 起 —— **BGE-M3 推理吃内存,4G 是下限不是舒适区**。
  4. **明确不做演示主路径。** 与 D9 冲突,且视觉缓存在 `data/` 里跟着机器走 ——
     本机焐热的那 22 分钟 VPS 上不认,要么再焐一次,要么同步几百 MB 的 `data/cache`。
     会场 WiFi / 跨网延迟 / 临时限流,任何一个都能毁掉演示。

## TODO-2 大文件异步任务队列(生产化)
- **What:** RQ/Celery + Redis,把超限文件(50-200MB 图纸、批量文档)改为后台异步处理 + 进度查询。
- **Why:** 真实工地图纸经常远超 20MB 上限,商用化必需。MVP 阶段 D7 已明确砍掉。
- **Pros:** 任意大小文件可收;架构更接近生产级。
- **Cons:** 新增 Redis+Worker 两个进程;约 3-4 天(人工)/ 1 天(CC)。
- **Context:** `ingest/` 离线脚本天然可改造成 worker 任务,升级路径已在架构中预留。
- **Depends:** 赛后;若做 TODO-3 应先于或同期。

## TODO-3 用户体系与多项目隔离(商业化前提)
- **What:** 登录/注册、项目级数据隔离、角色权限(工人/安全员/项目经理)。
- **Why:** 真实交付必需;按项目计费的前提。MVP 阶段 D10 已明确砍掉。
- **Pros:** 产品形态完整,可对外交付。
- **Cons:** 3-5 天(人工)/ 1-2 天(CC);全部数据表加 scope 字段。
- **Context:** 本期建表规范已要求预留 `project_id` 字段(成本≈0),未来迁移平滑一半。
- **Depends:** 赛后;建议先于 TODO-2 或同期。

## TODO-4 DWG 自动转换
- **What:** 服务器安装 ODA File Converter,DWG 上传后自动转 DXF 进入解析管道(ezdxf `odafc` 插件已支持)。
- **Why:** 工地实际图纸几乎全是 DWG;MVP 当前为「请先离线转换」引导话术(D6 深度上限)。
- **Pros:** 去掉真实用户体验的第一块短板。
- **Cons:** ODA 跨平台安装/打包麻烦;转换耗时需配合异步。约 1-2 天。
- **Context:** 演示图纸已离线预转,比赛不受影响。
- **Depends:** 建议排在 TODO-2 之后。

---

> 以下由 T1 骨架交叉核对 / 三视角评审(2026-08-06)产出。已在代码里就地修掉的不再列,
> 这里只留**需要人拍板或超出 T1 范围**的欠账。

## ~~TODO-5 让重试与缓存真正落在 Agent 执行路径上~~ ✅ 已收口(2026-08-06,W2 前置)
- **结论:选了方案 (b)** —— `langchain_core.globals.set_llm_cache` + 自实现 `BaseCache`
  (`llm.GytDiskCache`,复用现成的 `_cache_stamp` / `_cache_read` / `_cache_write`),
  由 `get_chat_model()` 幂等装载。`base_agent.py` / `graph.py` 一行未改。
- **为什么不是 (a):** 三组探针实测(见交付说明)表明 —— ① chat-ui 是流式的,而缓存查询在
  `_agenerate_with_cache` 里发生在流式分支**之前**,(b) 天然覆盖 astream / astream_events /
  `stream_mode="messages"` / v2 协议四条路径;② `llm_string` 自动含本次绑定的工具集,
  (a) 得手工把工具揉进键,漏一次就是「safety 的答案被 report 取走」,比没缓存更糟;
  ③ (a) 要覆写 `_agenerate` + `_astream` + `bind_tools` + `_get_ls_params` +
  `with_structured_output` 五个面,两人团队养不起。
- **额外必须做的一件事:** 算键前给 prompt 做归一化(`llm._strip_prompt_noise`)。langchain
  命中缓存时会往 `AIMessage` 上盖 `usage_metadata.total_cost=0`,这条被改过的消息进入下一轮
  prompt 会让第 2 轮键对不上 —— 不归一化的话彩排要跑三遍缓存才收敛。
- **验收:** `backend/tests/unit/test_llm_cache.py`,数的是**内层模型被调了几次**。
  已做变异测试:摘掉装载 / 摘掉归一化 / 键里去掉工具集,分别有 5 / 1 / 1 条用例转红。
- **剩下的欠账(转 TODO-9):** 自研退避的中文错误文案仍只在「直调」路径(`llm.ainvoke`)上生效;
  Agent 路径的重试由 `ChatOpenAI.max_retries` 承担,用户看到的是 openai SDK 的英文异常。
- **演示 checklist 新增一条:** `llm_string` 含 `request_timeout` / `max_retries` / `extra_body`,
  **彩排与演示的 `.env` 必须逐字一致**,改一个数字整份彩排缓存作废。
- **Context:** 完整说明与 ASCII 图在 `backend/src/gyt/core/llm.py` 顶部「图 0 / 图 0.5」。

## TODO-9 Agent 路径上的错误文案中文化(TODO-5 的剩余部分)
- **What:** `llm._classify` / `_user_msg` 那套「工人看得懂的中文人话」目前只覆盖 `llm.ainvoke`
  这条直调路径。Agent 路径的异常直接从 openai SDK 冒上来,是英文 traceback。
- **How:** 在图的出口(`graph.py` 或一层 middleware)统一捕获并过一遍 `_classify` / `_user_msg`,
  而**不要**再往模型层包一个代理 —— 那正是 TODO-5 评估后否掉的方案 (a)。
- **Cons:** 约半天。**Depends:** 无,可与 W2 并行。

## TODO-10 路径乙的重试是两层叠加的(自研退避 × SDK 退避)
- **What:** `llm.ainvoke`(路径乙,评测打分 / 知识综合的单次抽取走它)外面有
  `_invoke_with_retry`(1 + `llm_max_retries` = 4 次尝试),而 `get_chat_model` 造出来的
  `ChatOpenAI` 自己还带 `max_retries=3`(openai SDK 的指数退避)。撞上 429 时同一次
  **逻辑**调用最坏发出 4 × 4 = 16 个 HTTP 请求;30 条视觉评测(6 分钱一次)最坏 ≈ 29 元,
  而且自研退避本身要睡 1+2+4 秒,再叠 SDK 的退避,CI 的 `timeout-minutes: 30` 很容易被吃穿。
- **为什么没在 `_for_direct_call` 里顺手关掉(2026-08-06 实测):**
  `max_retries` 是在 `validate_environment` 里被烤进 openai 客户端的
  (`langchain_openai/chat_models/base.py:1265`),而 `model_copy` 不重跑它 ——
  副本用的还是同一个客户端。实测字段 3 → 0,但 `root_async_client.max_retries` 仍是 3。
- **临时办法:** 路径乙的调用方在**造模型时**就传 `get_chat_model("text", max_retries=0)`,
  让「自研退避 + 中文文案」成为唯一那层;路径甲维持 SDK 自带重试不变(那是它唯一的一层)。
- **进度(2026-08-07):Safety 这条路径已按临时办法收口。**
  `agents/safety/tools.py` 里写的是 `get_chat_model("vision", max_retries=0)`,
  并由 `tests/unit/test_safety.py::test_视觉调用走vision档且传了max_retries为0` 锁住 ——
  这行**看不出**有什么用,删掉也不会报错、不会变慢,只会在月底账单上多一个零,
  所以必须有测试守着。视觉档是三档里最贵的($3/M),它先收口收益最大。
  **仍未处理:** ~~评测 runner 与知识综合(队友泳道)的路径乙调用方还没传这个参数。~~
  **⚠️ 更正(2026-08-11 全仓核实):这两处路径乙调用方现在压根不存在,不是「还没传」。**
  全仓 `llm.ainvoke` 只有一个调用点 —— `agents/safety/tools.py`,而它上方已经是
  `get_chat_model("vision", max_retries=0)`。评测侧 `eval/hooks.py` 全文没有
  `get_chat_model` 也没有 `ainvoke`(`run_safety_row` 直调 `analyze_site_photo`、
  `run_rag_row` 直调 `search_regulation`,都不过模型);knowledge 侧也没有综合环节的直调。
  **所以这条现在是「零欠账、留个约定」:将来谁真写出第二个路径乙调用方,照抄 safety 那行即可。**
- **根治:** 把重试整体下沉到模型层(与 TODO-9 同一次改动里做最省事)。
- **Depends:** 无,但**在评测真正开始烧额度之前**必须处理。

## TODO-6 API Key 字段改用 `pydantic.SecretStr`(需改契约,要全员拍板)
- **What:** `Settings.deepseek_api_key` / `moonshot_api_key` 目前是裸 `str`。
- **已做的止血:** 两个字段加了 `Field(repr=False)`,`_ProviderConfig` 覆写了 `__repr__`,
  所以 `print/log(get_settings())` 不再泄明文 —— 这条泄露链(stdout → `docker compose logs`
  → CI 里 `cat langgraph.log` → 公开 Actions 日志)已经堵死。
- **仍缺的:** `model_dump()` 依然带出明文。
- **为什么没直接改:** 共享契约 v1 明文规定「字段名/类型/默认值一个字都不能改」,
  换成 `SecretStr` 属于改类型,要同步改所有取值点(`core/llm.py` 的 `_resolve_provider`
  与 `if not provider.api_key`,以及 `_ProviderConfig.api_key` 的标注)并更新契约文档。
- **Depends:** 越早越好,取值点越多越难改。

## TODO-7 LangGraph 服务端鉴权
- **What:** `backend/langgraph.json` 没有 `"auth"` 键,代码里也没有任何鉴权/限流,
  即 `:2024` 是一个零鉴权的 Agent 执行端点。
- **已做的止血:** `docker-compose.yml` 的端口发布已钉死 `127.0.0.1`,不再暴露给局域网。
- **仍缺的:** 一旦需要别的设备访问(手机扫码演示、云部署 TODO-1),就必须加
  `backend/auth.py` 并在 `langgraph.json` 里声明 `"auth": {"path": "./auth.py:auth"}`,
  用共享 token 挡住,顺带给 `/runs/*` 加个简单限流(`supervisor_recursion_limit`
  只限单次 run 的步数,不限 run 的次数,挡不住循环刷额度)。
- **Depends:** TODO-1 的硬前置。

## TODO-8 LLM 缓存条目加 HMAC 签名
- **What:** 缓存正文现在会连同答案回写 `meta`(键/模型/提示词版本/消息摘要/**答案摘要**),
  读侧逐项核对,能挡住「张冠李戴」「整份伪造」「改正文不改 meta」三种。
  (`answer_digest` 是 2026-08-06 补的:前四项全是关于**问题**的,一项都不核对答案 ——
  在补它之前,把 `message.content` 换掉、meta 一个字节不动,伪造内容就会零调用原样流出,
  连编 meta 都不用。)但仍挡不住「能改文件的人连 answer_digest 一起重算」。
- **Why:** `data/` 是宿主机绑定挂载,彩排缓存还会被打包在队员之间传递。
- **How:** 对整份 payload 做 HMAC,密钥从 `Settings` 取且**不落在同目录**。
- **Depends:** TODO-5 之后(缓存真正接进执行路径后这条才有实际意义)。

## TODO-11 视觉调用慢一个量级,演示日必须预热缓存
- **实测(2026-08-07,真调 kimi-k3 三张):**

  | 照片 | 尺寸 | 耗时 | 判断内容 |
  | --- | --- | --- | --- |
  | photo_29 办公室 | 1600×1067 | **10.1 秒** | 一眼判定不是工地 |
  | photo_13 工地 | 440×293 | **42.9 秒** | 要逐项分辨违规 |
  | photo_27 工地 | 4000×2430 | **59.7 秒** | 手机原图尺寸,已超 4K 线 |

  延迟**同时**受尺寸与判断复杂度影响 —— photo_29 分辨率最大却最快,是因为
  「不是工地」一眼可判;而 photo_27 两个因素叠满。
  (第一次只跑了 photo_13 就下结论说「延迟由复杂度决定、与尺寸无关」,是错的。)
- **已处理:** `llm_timeout_s` 60 → **150**(config.py,**对契约 v1 的刻意偏离,需团队追认**)。
  60 秒不是"余量小",是 photo_27 只剩 0.26 秒 —— 演示日随便一张手机照片就 TIMEOUT。
- **缓存是延迟的真正解:** 第二次调用 **0.005~0.037 秒**,结果逐字一致(三张都验过)。
- **演示日铁律(与 TODO-5 那条 `.env` 铁律同级):**
  1. 彩排时把演示要用的每张照片都真跑一遍,把缓存焐热;
  2. 焐热之后**不许再动 `vision_prompt.md` 的正文、`prompt_version`、或视觉型号** ——
     视觉缓存的键 = (model, prompt_version, messages, extra),提示词正文在 messages 里,
     动任何一项整份彩排缓存作废,演示当场退回 60 秒/张;
  3. `data/` 是绑定挂载,缓存要跟着演示机走,别在别的机器上焐热;
  4. 缓存是**字节级精确匹配**:现场临时拍的新照片必然 miss。演示脚本要照着焐过的图走,
     想现场拍就得接受 60 秒等待(或先按 TODO-12 降采样把延迟压下来)。
- **⚠️ 一处已更正的错误认识:** 先前写的「改 `llm_timeout_s` 会让全部缓存失效」
  **对视觉缓存是错的**(2026-08-07 实测:timeout 60→150,路径乙缓存键一字未变)。
  两条路径的键构成不同 —— 路径甲(Agent 对话)的键含 `llm_string`、里面有 `request_timeout`;
  路径乙(视觉直调)的键不含 timeout。据此定的"必须赶在彩排前改 timeout"其实不成立,
  但改 timeout 本身仍然是对的(见上)。config.py 该字段上方的注释已同步更正。
- **待办:** ① TODO-12 的降采样(4K→2K)做完后延迟应显著下降,届时可回调 `llm_timeout_s`,
  但**必须赶在彩排开始之前**;
  ② 评估视觉档要不要也关思考模式(现在只有 text 档关,见 `llm.get_chat_model`),
  但视觉判断可能真需要推理,**要用评测分数说话,不能拍脑袋关**。
- **Depends:** 无。演示前必做。

## ~~TODO-13 hard 桶样本上模型会做「场景豁免」推理,标注口径要对齐~~ ✅ 已裁决(2026-08-08)
- **实测(2026-08-07,photo_27):** 草稿标注是 `violation / 未穿反光衣`,
  模型答的是 `compliant`,note 原文:「两名作业人员均戴蓝色安全帽、着工装手套,
  在倒伏的电线杆上抢修;周围是洪水冲积的树枝杂物而非施工材料,
  **未穿反光背心但属野外抢修场景故未报**;电杆倒伏处是否有导线泡水看不清,建议人工复核」。
- **What:** 模型做了一次超出提示词预期的推理 —— 它自己判断「野外抢修 ≠ 工地施工,
  因此不要求反光衣」。这不是幻觉,是有依据的场景判断,但和标注口径不一致。
  同类情况还有 photo_13:模型只报「未穿反光衣」,把远处未戴安全帽的人以
  「更像路过行人,身份不清」为由放过 —— 那正是 vision_prompt.md 要求的
  「看不清就不报」,口径生效了,但在 violation 行会算漏报。
- **要决定的:** 保守口径(准确性优先)在 violation 行必然造成漏报,而判分是
  集合完全相等、不给部分分。两条路二选一,**都要用 30 张跑完的分数说话**:
  ① 维持保守口径,接受 violation 行的漏报,靠标注时也采同样口径来对齐;
  ② 放宽成「看到就报,不确定的也报但在 note 标注」,接受 compliant/not_site 行的误报风险。
- **为什么现在不动:** 只跑了 3 张,样本太少。改提示词还会让彩排缓存作废(TODO-11)。
  **先把 27 张标注完、跑一轮完整评测拿到分数,再决定。**
- **进展(2026-08-08,not_site 口径重写后的第 5 轮评测):** 口径已按负责人要求改为
  「拿不准按作业现场从严」。新的争议行全部是**多报方向**,且疑似又是标注缺项:
    S04 +未穿反光衣(盲审记录里写着「穿蓝色工装的工人沿板边行走」—— 蓝工装即无反光衣)
    S11 +临边无防护(选图时就人工发现过「人站在完全无栏杆的楼板边缘」,标注未收)
    S14 +临边无防护 / S27 +高空作业未系安全带(未复核)
  下次盲审把这 4 行一并裁决 —— **尺子疑似再次落后于模型,该修标注不是提示词。**
  另:kimi-k3 视觉端固定 temperature=1.0(官方锁死),评测有 ±1 行固有噪声,
  对比两轮分数时差 1 行以内不作数。
- **✅ 裁决(2026-08-08,人工逐张看图,3 补 1 驳):** S04 补「未穿反光衣」(顶板人员
  全深色工装)、S11 补「临边无防护」(无栏杆挑檐带)、S14 补「临边无防护」(平台边缘
  无护栏);**S27 驳回** —— 放大确认人在地面(洪水压平的田地)弯腰作业,「高空」是
  模型真实误报,标注不改。裁决后 80.0% → **90.0%(27/30),重合度 96.6%**。
  剩余 3 条失败定性:S01 漏报竹脚手架的临边(非楼层结构的临边口径仍有歧义)、
  S07 漏报前景散落钢筋、S27 误报高空 —— 都是真实模型误差,不再动尺子。
- **Depends:** 标注完成 + 首轮 `make eval SUITE=safety`。

## ~~TODO-12 动图会绕过格式白名单,被当视频计费~~ ✅ 已收口(2026-08-07)
- **What:** animated GIF / animated WebP 的 MIME 就是 `image/gif` / `image/webp`,
  **与静态图完全相同**(magic bytes 也一样)。`ALLOWED_IMAGE_EXT` 里有 `.webp`,
  所以 `agents/safety/tools.py` 的格式校验对动图**必然放行**。
  而月之暗面官方说动图**可能**被当作视频解码并按视频计费 token —— 一张动图的账单
  可以是静态图的几十倍,且完全静默。
- **为什么现在没修:** 挡动图只能读帧数(`PIL.Image.n_frames`),
  而 backend **没装 Pillow**(实测 `ModuleNotFoundError: No module named 'PIL'`)。
- **How:** 加 Pillow 依赖后,在 `analyze_site_photo` 的格式校验之后补一道帧数探测,
  `n_frames > 1` 就 `fail(FILE_UNSUPPORTED)` 或抽首帧。
- **顺带做掉的:** Pillow 一旦装上,同一处还能补两件事 ——
  ① **降采样**:图片按像素面积计 token,手机原图贴着 4K 线,
     官方建议先 resize 到 1080p~2K,这是真金白银的省(评测集的图都 ≤1600px,不受影响,
     所以这条只影响**演示**,不影响评测分数);
  ② **真伪校验**:现在只看扩展名,一个改名成 .jpg 的文本文件照样会被 base64 发出去。
- **已做(2026-08-07):** 装了 Pillow,在 `agents/safety/tools.py` 加了 `_prepare_image`,
  一次解决三件只能靠**解码图片本身**才能做的事:
  ① **降采样** —— 长边超 `photo_compress_max_edge_px`(2048)就等比缩小;
     体积仍超 `photo_compress_target_mb`(4MB)再逐档降 JPEG 质量(85/75/65/55)。
     这两个配置项此前定义了却**全仓零引用**(审查 TODO-17 提过),现已接上。
  ② **挡动图** —— `n_frames > 1` 直接 fail(FILE_UNSUPPORTED)。
  ③ **验真伪** —— Pillow 打不开就 fail(FILE_CORRUPT)。此前只看扩展名,
     一个改名成 .jpg 的文本文件照样会被 base64 发出去,白花一次钱换回一句听不懂的话。
- **实测(30 张评测集):** 只有 3 张超限被处理,但降幅很大 ——
  photo_09 3671×4923→1527×2048(像素 -83%)、photo_27 4000×2430→2048×1244(-74%)、
  photo_30 1600×2400→1365×2048(-27%);总像素 64.9→41.8 Mpx(**-36%**)。
  视觉 token 按像素面积计,所以这约等于输入 token 的降幅。
  **对演示的意义更大**:photo_27 正是手机原图那个量级,而实测这类图端到端要 59.7 秒。
- **精度零损失(必须实测才敢说):** 降采样后重跑评测,得分 83.3%、逐类召回、
  平均重合度 95.2% **一个数字都没变**。没有这一步验证,降采样就是在拿识别率换钱。
- **没做的:** 没有为了省钱把质量压到 55 以下 —— 判断「有没有戴安全帽」「腰上有没有挂钩」
  靠的是细节,漏报一个隐患的代价远高于几分钱。上游真正的硬限是请求体 100MB,离得很远。

---

> 以下由 Safety Agent 四路审查(2026-08-07,40 个 agent、含对抗证伪)产出。
> 36 条发现里存活 29 条。**已就地修掉的不再列**,这里只留还没做的。
> 完整原文在 /private/tmp/.../tasks/wtblalwaq.output(会被清理,重要的都摘到下面了)。

## ~~审查发现:已修的四条~~ ✅(2026-08-07)
- **解析不出的回答被写进缓存** —— 那句「请再试一次」永远不可能成功:第二次直接命中坏缓存、
  0 次网络调用、一字不差的同一句失败。而缓存键算在**图片内容**上,让用户重传同一个文件
  也逃不掉(新 artifact_id、同一个键)。唯一的清理手段会烧掉整份缓存,与 TODO-11 的预热正面冲突。
  → `llm.ainvoke` 加可选的 `is_usable` 闸门(默认不传=原行为,对既有调用方零影响),
  safety 传「抠得出 JSON 才算可用」。锁在 `test_解析不出的回答不写缓存_重试真的能成功`。
- **`_OBJECT_RE` 贪婪匹配**把「答案 + 后面一句带花括号的说明」整段吃掉判成失败 →
  改成花括号配平扫描、逐段尝试,并跳过字符串字面量内的花括号与转义。
- **summary 把「并给了照片编号」写成路由前提** —— routing.csv 里 safety 那些行原文都不带编号,
  supervisor 会自己反问而不派活。缺编号的追问归 safety/prompt.md 管,不该在路由层重复把关。
- **视觉档重试 4 次 × 150 秒 = 607 秒黑屏**(这个洞是把 timeout 从 60 提到 150 时**我自己放大的**)
  → `ainvoke` 加 `max_attempts`,视觉档压到 2 次。

## TODO-14 提示词与代码的「契约同源」只守住了一半
- **已做:** 加了 `OUTPUT_KEYS` 常量 + `test_视觉提示词必须写明输出契约的三个字段名`。
  没有它的话,把 vision_prompt.md 里的 `violations` 改成 `items`,**全部测试照绿**,
  而每张违规照片都会被讲成「没看到明显的安全问题」—— 安全产品最坏的静默漏报。
- **仍缺的三处(审查逐条给了改法,照做即可):**
  1. ~~`scorers.py:31` 与 `eval/README.md:83` 的指路牌仍写着 `agents/safety/prompt.md`,
     而词表已经搬到 `vision_prompt.md` 了 —— 照着指路牌去改的人会改错文件。~~
     **✅ 已改(2026-08-11)。** 当时漏登记了**第三处**:`CLAUDE.md` 的同源清单也指着
     `agents/safety/prompt.md`,而且它是这条链上唯一给出具体路径的地方。三处一起改掉了。
  2. 词表测试只断言「VIOLATION_VOCAB ⊆ 提示词」这**一个方向**,且完全没读 eval/README.md。
     应改成三方**集合相等**:提示词表格解析出的词集 == README 代码块里的词集 == VIOLATION_VOCAB。
     **仍缺。**(2026-08-11 复核:三处内容目前**逐字一致**,但那是人工对齐的结果,没有守卫。)
  3. 三个 label 取值(violation/compliant/not_site)同样没有守卫。**仍缺。**
     顺带记一笔同一类的洞:`validate_safety_row` **完全不校验 `type` 列** ——
     README 曾把取值域写成 `positive`,而数据里 16 行用的是 `violation`,
     漂了整整一轮**没有任何一条测试变红**(已按数据实况改了 README)。
- **Depends:** 无。改词表之前必须先补 2、3,否则守卫是假的。

## TODO-15 视觉档没关思考模式 —— 可能比降采样更值钱
- **审查测算:** 输出 token 里约 93% 是 reasoning,吃掉约 61% 的钱与 57% 的延迟。
- **验证方法(便宜,约 $0.07):** 用
  `get_chat_model("vision", max_retries=0, extra_body={"thinking": {"type": "disabled"}})`
  跑 photo_13 与 photo_27,对比 output_tokens 与墙钟。
  ⚠️ **这个数字是审查 agent 自己测的,我没有独立复核过** —— 落地前先自己跑一次 A/B。
- **但不能只看快慢:** 关掉思考很可能**降低判断质量**,而 hard 桶的样本(见 TODO-13)
  恰恰依赖推理。**必须用 30 张的评测分数一起看**,不能只因为快就关。
- **配套:** 若确认要关,`llm.get_chat_model` 现在只在 `purpose == "text"` 时注入
  thinking disabled,要改成按档位配置而不是写死。
- **Depends:** 首轮完整评测。与 TODO-13 同一批决策。

## TODO-16 密钥类故障只挡住了「没填」这一种
- **What:** `tools.py` 显式接住了 `MissingAPIKeyError`(Key 为空),但注释里自己列的
  另外三种 —— Key 填错(401/403)、账号没充值导致 kimi-k3 未解锁、额度耗尽(429/402)——
  全部落回通用文案。尤其**额度耗尽若被上游用 429 表达**,现有文案会说
  「现在用的人太多,正在排队,请过一会儿再试」,而正确动作是**去充值**。
  演示日照着这句话干等是最坏的结果。
- **How:** ① `llm.py` 加 status→文案覆盖表:401/403 指向「密钥无效或账号未开通 kimi-k3」;
  429 先看响应体有没有 `exceeded_current_quota` / `insufficient_balance`,命中就判**不可重试**;
  402 加进 `_FATAL_STATUS`。② `tools.py` 把注释改成实话(这里只挡 Key 为空)。
- **Depends:** 无。演示前必做。

## TODO-17 审查剩余的 medium / low(12 + 4 条,逐条已有改法)
挑几条影响最大的,完整清单见审查输出:
- **`docs/W2_执行计划.html` 仍加粗要求 safety 用 `purpose="vision"`**,与本次已定架构完全相反 ——
  队友照着文档做会走反方向。
- **`photo_compress_target_mb` / `photo_compress_max_edge_px` 定义了但全仓零引用**,
  造成「降采样已实现」的错觉;W2 计划 S1 的验收门槛「超限照片自动压缩后能通过」既没兑现也没记账(并入 TODO-12)。
- **`_BYTES_PER_MB` 的量纲无测试守护**:写成 1KB 的话 CI 全绿,而演示日每张手机照片都被拒。
- **两条缓存测试无法区分「按图片内容做键」与「按 artifact_id 做键」** —— 后者会静默废掉
  TODO-11 的演示日预热(每次 register 都发新 uuid4)。要补一条「同一份图片重新上传、新编号照样命中」。
- **`_summarize` 无视 label**:violations 非空就宣布「发现 N 处问题」,
  label=compliant/not_site 时会给工人假警报。
- **工具入参校验层没测**,pydantic 的英文报错会绕过 `tool_guard` 的信封契约直接送给 Agent。
- **单元测试真的能连外网**:conftest 承诺「绝不放行到外网」但没有任何东西强制。
- ~~**`graph.py` 顶部自称「此刻真正构建出来的东西」的拓扑图里没有 safety。**~~
  **◐ 一半已收口(2026-08-11 核实):**那句「此刻真正构建出来的东西」全仓已经搜不到,
  「图 1」现在明写自己是**历史快照**、并把人指向 `AGENT_REGISTRY`。
  **但紧随其后的「图 2」还画着 ping 在挂载位上、knowledge/cad 还标着「队友 · W2」** ——
  三个都过期了(ping 已摘除,那两位 2026-08-09 已在册)。原诉求的弱化版本仍成立。
- **`backend/README.md` 的路径乙范例没带 `max_retries=0`** —— 队友照抄的正是这段。

## TODO-18 评测集类别覆盖 —— **已从 2 类扩到 7 类,只剩「消防通道堵塞」零样本**
- **✅ 大部分已收口(2026-08-07,commit `4b60876`「换掉 11 张非工地照片,覆盖面从 2 类扩到 7 类」)。**
  2026-08-08 复核 `safety.csv`(30 行,violation 17 / compliant 8 / not_site 5),
  八个受控词的实际样本数:

      未戴安全帽 7 · 未穿反光衣 12 · 临边无防护 7 · 高空作业未系安全带 4
      材料堆放混乱 2 · 用电隐患 2 · 动火作业无监护 2 · **消防通道堵塞 0** ← 仅剩这一个

- **仍要做的决定(二选一,演示前):** ① 补 1~2 张消防通道堵塞的照片(来源守
  `data/demo/README.md` 的硬规矩,搜索词 blocked fire exit / obstructed emergency exit),
  补完把 `eval_min_rows_safety` 从 30 提到 31~32;
  ② 或者**把这个词从受控词表里删掉** —— 留着一个从未被验证、演示也演不了的词,
  比没有它更糟:它给人「这个功能有」的错觉,而模型有动机去凑那个词(误报方向)。
  删的话 README / `scorers.VIOLATION_VOCAB` / `vision_prompt.md` **三处一起改**(见 TODO-14),
  且改 `vision_prompt.md` 正文会**烧掉整份视觉缓存**(TODO-11),要赶在焐热之前。

- **以下为当初的根因记录(2026-08-07),保留备查:**
- **What(2026-08-07 标注定稿后发现):** `safety.csv` 30 行里用到的违规词只有
  **未戴安全帽 / 未穿反光衣**。另外 6 个受控词一次都没出现过:

      临边无防护 · 高空作业未系安全带 · 材料堆放混乱
      消防通道堵塞 · 用电隐患 · 动火作业无监护

- **根源:** 源数据集(Roboflow construction-safety-monitor)只标 PPE ——
  helmet / vest / boots / gloves / goggles,**没有任何场景类隐患的标注**。
  我们是从它的 5170 张里挑图的,挑不出没有的东西。
- **后果(别把分数读大了):** 这一轮分数只能说明「PPE 识别准不准」,
  **不能**说明「8 类违规识别准不准」。而 `vision_prompt.md` 里那 6 个词
  从未被验证过 —— 模型对它们是压根不报(漏报)还是见风就报(误报),目前**完全未知**。
  演示日若评委拿一张「材料堆放混乱」或「消防通道堵塞」的照片来试,结果不可预测。
- **为什么这比听起来更要紧:** 误报方向尤其危险。提示词把 8 个词都列了出来,
  模型有动机去凑 —— 而现有 30 张里没有任何一张能把「乱报临边无防护」这种行为测出来。
- **How:** 补 6~10 张覆盖其余类别的照片(每类至少 1 张,理想是 1 正 1 负)。
  这类图在 PPE 数据集里找不到,要另找来源 —— 建议按 `data/demo/README.md`
  那条硬规矩走(只收 Pexels / Unsplash / Pixabay / Wikimedia CC0,
  禁从 Google 图片扒 CDN)。搜索词:scaffolding edge protection、
  blocked fire exit、construction material stacking、welding hot work、
  exposed electrical wiring construction。
  补完 `eval_min_rows_safety` 也要跟着提(现在 30,补到 36~40)。
- **要一起想清楚的:** 若某几类实在找不到合适照片,应当考虑**把那几个词从受控词表里删掉** ——
  留着一个从未被验证、也演示不了的词,比没有它更糟:它给人「这个功能有」的错觉。
  词表要改,README / scorers.VIOLATION_VOCAB / vision_prompt.md **三处一起改**(见 TODO-14)。
- **Depends:** 首轮评测出分之后。先看 PPE 这两类的分数,再决定投多少精力扩类别。

## ~~TODO-19 【首轮评测的真正结论】数据集选型错了 —— 45% 的候选根本不是工地~~ ✅ 已重筛(2026-08-07)
- **✅ 收口:** commit `4b60876`「换掉 11 张非工地照片,评测覆盖面从 2 类扩到 7 类」——
  正是本条点名的那 ~11 条。之后分数一路 46.7% → 63.3% → 80.0% → 83.3% → **90.0%(27/30)**,
  门槛 80% 已过。**下面的根因分析保留备查,尤其是那条方法论教训(凡「按标签筛数据」
  的步骤都必须抽查若干张看图再往下走)—— 队友做 knowledge/cad 的数据集时会踩同一个坑。**
- **首轮评测(2026-08-07):46.7%(14/30),FAIL。但这个分数不反映模型能力。**
  16 条失败拆开:**~11 条是数据集根本错了**(非工地照片被标成工地违规)、
  4 条是判断口径分歧(TODO-13)、1 条 TIMEOUT。
- **实证:** 逐张复查 + 抽样调查(`survey.py`,固定种子 20260807,20 张有效样本):

      violation 候选池里真正是建筑工地的只有 **45%**

  非工地的类型:工厂车间×3、**白底棚拍人像×3**、实验室、木工房、室内粉刷、
  VR 摆拍、已建成厂房内查看。评测集里已混进来的:动物园「BEWARE TIGERS」游客、
  俄语采访摆拍、"Rural Women" 儿童活动、学童路队、木工房、机械修理厂。
- **根因(不是数据质量问题,是选型问题):**
  这个数据集的**收集标准是「画面里有人穿戴/未穿戴 PPE」,不是「这是工地」**。
  它是 PPE 检测数据集,名字里的 construction 是应用场景不是内容约束。
  而 `prefilter.bucket_of` 完全按源标注的类别分桶(有 no-helmet 就进 violation),
  源标注只回答「这个人戴没戴安全帽」,**从不回答「这是不是工地」**。
- **我的方法论错误(值得记住):** prefilter 做了 dry-run、看了分桶数量、
  按第 10 百分位调了阈值、做了分层采样 —— **唯独一张图都没抽查过**。
  验证了统计分布,没验证语义。数字全部合理,内容全是错的。
  凡是「按标签筛数据」的步骤,都必须抽查若干张**看图**再往下走。
- **模型其实表现很好(别被分数误导):** 它的理由精准到能认出
  「泰英双语 BEWARE TIGERS 警示牌」「Rural Women 社区活动」,
  还会区分「保安位于门口外围、非作业区内,故未报未戴安全帽」——
  它理解的是违规的**语境**,不是机械匹配 PPE。
- **How:** 给 `prefilter` 加一道「是不是工地」的筛选(用视觉模型判,提示词见 survey.py),
  重筛 violation 与 hard 两个桶(compliant 桶污染轻:7 张评测全对,
  因为它的判据是「≥2 个正向 PPE 框」,真工地更容易满足)。
  按 45% 命中率,筛约 30~40 张候选就能凑够 13 张真工地违规图。成本约 2~3 元。
  **筛完仍需人工标违规项** —— 模型只用来判「是不是工地」这个客观场景问题,
  不用来定违规标准答案(那是自己给自己判卷)。
- **Depends:** 无。**这是 W2 safety 验收的头号阻塞项** —— 数据集不干净,
  分数没有任何意义,也不该据此调提示词(会把两个问题搅在一起)。

## TODO-20 Kimi 组织级并发上限是 3
- **实测(2026-08-07):** `survey.py` 设 5 并发,20 张里 8 张被打回:
  `429 ... request reached max organization concurrency: 3, please try again after 1 seconds`
- **影响:**
  ① 评测想并发提速,**上限就是 3**(现在 `run_suite` 是串行,30 张约 22 分钟;
     3 并发理论上能压到 ~8 分钟,但要留余量,建议 2);
  ② **演示日若多人同时用会互相挤掉** —— 队友在旁边跑评测、评委同时扫码体验,
     就会撞 429。演示前要约定好「演示期间不许跑评测」;
  ③ 429 在 `llm._classify` 里是**可重试**的,叠加退避会显著拉长总时长。
- **How:** 若要给 `run_suite` 加并发,信号量上限写 2、且做成配置项;
  同时把这条写进演示 checklist。
- **Depends:** 无。加并发之前必须知道这个数。

## TODO-21 Safety 五场景落位(2026-08-08 与负责人对齐)——哪些演、哪些做、哪些明确不做
- **背景:** 负责人给出五个真实使用场景,要求 Agent「具备使用场景」。逐一落位如下,
  **这是范围决定,改之前先跟两个人都打招呼:**
  1. **工友随手拍上报 —— 已完成,演示主打。** 上传→自动登记→判读→定级→中文人话,
     就是 2026-08-07/08 修通的那条链。演示话术:「工友不需要懂规范、不需要填表」。
  2. **安全员日常巡检 —— W2 完成识别+定级,条文/派单/期限属 W3 联动**
     (knowledge 出依据、schedule 排期限,走英雄链与真实路由,均在原规划内)。
     → 2026-08-08 更新:schedule 侧已上线,真机验证过「巡检→一句话记整改任务(带期限)」
       的协同一手,零联动代码;knowledge 出依据仍待队友泳道。
     → 2026-08-09 更新:**knowledge 也上线了**(commit `9b120be`)。`search_regulation`
       的成功回执带《文件名》+ 页码,「出依据」这一半到位;本条第 2 项的两个缺口都补齐了。
       (2026-08-11 补记 —— 上面那行 08-08 的原话留着不改,只往下续。)
  3. **高风险作业开工前确认 —— 不写新代码,用现有能力演一半:**
     evaluation 集 S23/S24(动火)模型输出的就是「无灭火器、无监护人」——
     这就是动火开工前核对的核心。**作业许可审批工作流是流程系统,赛后。**
  4. **整改闭环验证(before/after 对比)—— W3 可选,约半天:**
     kimi-k3 一条消息可收多图,compare_rectification(before_id, after_id) 技术可行。
     先把英雄链跑通再说,别抢 W3 主线。
  5. **极端天气 + Weather agent —— 明确不做(赛后):**
     Weather agent 不在整个 6-Agent 规划里,第四周也做不完。演示时可口头提为路线图。
- **已落地的部分(本次):** 隐患定级(一般/较大/重大)进了工具信封 ——
  `agents/safety/severity.py` 确定性映射表,**放代码不放模型**
  (可解释/可校准/可测试,且不动提示词=不烧视觉缓存)。
  信封 data 五键(label/violations/severity/max_severity/note)已声明为
  **safety → report 的交接契约**,W3 的 Report/Schedule 按这个取数。
- **定位声明(边界①,已写进 prompt.md):** 初筛与记录,精确测量与资质判断归持证安全员。
  **技术说明文档里要原样写这三句**(负责人原话认可「主动写进技术说明是加分项」):
  ① 模型对显性大目标(安全帽/反光衣/堆料)可靠;② 对需精确测量或专业判断的
  (锚点合格性/栏杆 1.2m/接地规范)不可靠;③ 故定位为初筛与记录,最终判定归持证安全员。
- **边界③(不做实时视频流)—— 与既有 D 决策一致,无需动作。**

## TODO-22 演示素材人脸打码 + 技术说明隐私段
- **What:** 评测/演示照片里有清晰人脸(工人、路人)。演示投屏与答辩材料要打码,
  技术说明里加一段隐私处理方案(上传即压缩、不外传、人脸模糊化路线)。
- **顺带化解一条真实条款风险:** Pexels License 禁止把画面中可识别人物用于
  "in a bad light or in a way that is offensive" —— 我们的用法恰恰是
  「把此人展示为安全违规案例」,处于灰区。**打码之后这条风险一并消失。**
- **How:** 演示片子/幻灯片手工打码最省事;产品侧若要自动化,Pillow 已装,
  但人脸检测要新增依赖,赛后再说。演示素材打码是**演示前必做**。
- **另发现(盲审时):** Roboflow 数据集里混有带图库水印的存量图 ——
  photo_14 带「全景网 quanjing.com」水印、photo_27 带「pic.cnhubei.com」水印。
  私有仓库内部评测风险低,**公开仓库(TODO-1)前必须替换这两张**(换图会让对应
  缓存失效,记得重新预热)。
- **Depends:** 彩排出片前。

## TODO-23 Schedule 落地交接备忘(2026-08-08,lawrence/schedule-agent)
- **routing 现状 15/22=68.2%(门槛 0.90):** R12~R14 翻绿、已绿行零回归。
  剩 7 条红全在队友泳道:knowledge×4(R08~R11)、cad×3(R15~R17),
  其中 6 条是「→none 如实说做不了」的正确失败模式。
  > **⚠️ 更正(2026-08-11,原句是 08-08 的交接记录、留档不改):这个数已被两次落地作废。**
  > cad(`b4fc154`)与 knowledge(`9b120be`)都在 2026-08-09 挂进了 `AGENT_REGISTRY`,
  > 那 7 条不再有结构性障碍,**当前结构上限 = 22/22**。最近一次留档是
  > **routing 22/22、rag 20/20**,写在 `9b120be` 的提交信息里;而 `graph.py` /
  > `routing.csv` / `scorers.py` 自那以后一行未改(`git diff 9b120be..HEAD --` 这三个路径为空),
  > 所以那次测量的输入今天仍成立 —— **但不等于今天重跑还是这个数**,要引用先自己跑。
- **R17(图纸柱距)仍误派 safety:** 等 cad 挂上正域 summary 自然收走。
  **别**往 safety 的 summary 里加「不看图纸」这类否定句去救 —— 否定句里的名词
  照样当钓饵(ping 三连教训),空等 cad 是对的。
  > **✅ 已收口(2026-08-11 核实):** cad 的 `AgentSpec` 正域 summary 已写足
  > (查尺寸 / 数构件在哪层 / 列图层清单 / 出 PNG 预览),`graph.py` 里那条注释也写着
  > 「routing R15-R17 的 expected_agent 已是 cad,这条一上线就把误派/空派归位」。
  > **「别改 safety 的 summary 去救它」这条禁令仍然有效**,别因为收口了就当它作废。
- **本轮为救回归在 supervisor 提示词加了规则 4 细化+规则 5(手嘴一致):**
  背景是关思考模式后模型把盘算写进正文、写完又补一个矛盾的交接调用
  (R21「派不下去」却 transfer_to_safety)。以后路由行为诡异,先查这两条规则动没动。
- **演示日台账重置:** `rm -f data/gyt.sqlite3`(仓库根的 data/,下次调用自动重建空表)。
  真机验证在库里留了 T1(已销)/T2(整改任务)两条,正式彩排前按需清掉。
  ⚠️ 这条原本写的是 `backend/data/gyt.sqlite3` —— 数据根目录统一到仓库根之后
  那条命令是**空操作**:删掉的是没人再读的历史遗留文件,彩排时以为清干净了,
  实际带着旧台账上台。老机器上如果 `backend/data/` 还在,顺手一起删掉别留着混淆;
  想留住里面预热过的 LLM 缓存,就先用 `backend/scripts/migrate_data_dir.py --apply`
  把它并进仓库根 `data/`(默认只演练不动手,跑法见脚本头注)。
- **日期词表 v1 冻结在 agents/schedule/dates.py 的 docstring:** 改词表要同步
  prompt.md 的日期红线与演示脚本;「本周X已过报错不猜」是有意设计,别当 bug 修。
- **当天下午真机测试追加的三条结构性教训(都有测试/脚本钉死):**
  ① `add_handoff_back_messages` 是**收工信号不是装饰**——关掉它 supervisor 会
     对同一件事复转直到熔断(graph.py 有血泪注释,英文观感由前端 ai.tsx 折叠解决);
  ② FocusOnOwnWork 滤网收窄为「只滤 supervisor 派活腔」——第一版连兄弟 Agent
     的成果也滤,巡检→记整改任务的协同当场断裂(core/focus.py 记录了案情);
  ③ **防假账结构件** agents/schedule/guard.py(RequireLedgerTool):多轮后模型
     会不调工具、照上文回执「补」一条(嘴上销了库里没销,库为证)——回合首答
     必须带工具调用,否则打回重试一次。提示词红线只是概率生效,结构件才兜得住。
- **真机验收脚本进仓库:** `backend/scripts/live_acceptance.py`(19 断言,
  含库级铁证组 D),演示彩排前跑一遍;跑法见脚本头注。
  (2026-08-10 已扩到 25 断言,补了 E/F 两组;剩下的欠账见 TODO-24 / TODO-25。)

## TODO-24 真机验收对 knowledge / cad 只验了一条最顺的路
- **What:** `backend/scripts/live_acceptance.py` 的 E 组(3 条)和 F 组(3 条)是
  2026-08-10 的 `ebf0b42` 补的(19 → 25 断言)。两组各自**只问了一个问题**,
  而这两个 Agent 的能力面比这宽得多。要把覆盖从「一条顺路」补到「顺路 + 该说不知道 + 会失败的路」。
- **E 组现状(knowledge):** 唯一那句问的是「消防车道的净宽度和净空高度有什么要求」,
  照抄 `eval/datasets/rag.csv` 的 **K01**。那份数据集一共 20 行(K01~K20):
  15 条 positive、1 条 multi_point(K15)、**4 条 no_answer(K17~K20)**。
  也就是说「**该承认查不到的时候承认查不到**」这一档,真机验收一条都没验 ——
  而这条措辞规则重要到已经进了 CLAUDE.md 的同源清单(`eval/scorers.py` 三张词表 +
  `eval/README.md` + knowledge 的 `prompt.md`)。knowledge 最容易砸的恰恰是它:
  库里没有,却硬编一条听着很像规范的话出来。
- **E3 那条库级断言堵不住这个洞。** 它验的是「向量库里有 chunk」+「入库清单里的 PDF
  还在演示资产目录」,**不验 E2 那个答案是不是真检索出来的**。脚本自己在 `count_chunks`
  的 docstring 里就写明了:「消防车道不小于 4 米」恰恰是模型预训练里就有的知识,最容易蒙对。
  → E2 和 E3 同时绿,仍然排除不掉「模型压根没检索、凭常识答对,而库恰好非空」。
  另外 `data/demo/docs/` 下当前**只有一份规范 PDF**(GB-50016-2014 建筑设计防火规范 2018 版),
  跨规范、跨文档的检索现在连验的条件都没有。
- **F 组现状(cad):** 唯一那句问的是「首层平面图有哪些图层」。`agents/cad/tools.py` 里有
  6 个工具 —— `list_drawings` / `parse_drawing` / `query_dimension` / `list_components` /
  `layer_stats` / `render_preview` —— F 组只走到 parse + layer_stats(F3 断的是本轮新写的解析索引)。
  查尺寸、列构件、出预览图、列图纸清单,一条都没验。
- **顺带漏掉的两条分支:** `data/demo/drawings/` 里有三张图,`names.json` 的映射是
  `plan_gbk.dxf`→首层平面图(**GBK** 编码)、`plan_utf8.dxf`→设备房平面图(**UTF-8**)、
  `broken.dxf`→损坏测试图。验收只碰了 GBK 那张 —— 中文图层名的编码分支只覆盖一半;
  而 `broken.dxf` 摆在那儿本来就是给失败路径用的,从来没被验收碰过一次。
- **不修会怎样:** 现在这 6 条绿只证明「最顺的那一问能答」。演示当场评委问第二个问题
  (换一份规范、问一句库里没有的、问尺寸、换一张图)全是裸奔 —— 而这些恰恰是
  最容易被问到的追问。
- **从哪下手(按性价比排,别一次加满):** 每加一问都是一次真调模型(几分钱、十几秒串行),
  优先级 ① E 组补 1 条 no_answer(K17~K20 任挑一条,判据抄 `eval/scorers.py` 的
  「承认查不到」词表,别另编一套);② F 组补一问**设备房平面图**(顺手压住 UTF-8 那条编码分支);
  ③ F 组补一问尺寸或构件;④ 损坏图纸的失败话术。
- **Depends:** 无硬依赖。E 组要求本机已建向量库(脚本头注第 0 步)。彩排前做完最划算。

## TODO-25 验收大半在「断措辞」,不是「断事实」
- **What:** 把 `backend/scripts/live_acceptance.py` 里「在回复文本里找关键词」的断言,
  逐条改成「去库里 / 盘上看事实」。D 组已经把做法示范完了,这条路要往 A/C/E/F 组推。
- **现状(25 条逐条数过,分三档;2026-08-10 W5·T6 之后重数了一遍):**
  · **断路由**(看有没有 `transfer_to_*`):B1 B2 C1 E1 F1 —— **5 条**。结构断言,模型换措辞不影响,是好的。
  · **断事实**(直查 sqlite / 看磁盘):D1~D4 + E3 + F3 —— **6 条**。
  · **断回复文本**:A1~A10 + C2 + C3 + E2 + F2 —— **14 条**。其中 **13 条是纯措辞**,
    **A10 已经是混合体**(见下),所以「超过一半靠措辞」这句仍然成立,但别再把 A10 算成纯措辞那一类。
- **脆在哪(判据原文):** A5 只认一个「销」字;A6 认「本来就 / 已经销 / 已经完成」三选一,
  且不许出现「开小差」;A8 认「明天」或「支持」。
  模型换个说法就红,而它的**行为**可能完全正确 —— 彩排前一小时刷出一片红,人会去改脚本
  或者改提示词凑关键词,那是在给验收注水。
- **⚠️ A10 的描述在 2026-08-10 当天就过期了,这里是订正后的实况**(旧文写「A10 认『已经过』
  或『过了』」,那只剩其中一支的一个子句)。A10 现在**按日历分两支**,两支都带结构判据:
  · **报错支**(今天周四~周日,「本周三」已过)6 个子句:短语回显 + 「已经过/过了」+
    改法提示(`D.a10_hint`,同源自 `dates.py:213-214` 抛出来的真异常原文)+ 不许「开小差」
    + **`| 任务号 |` 不许出现在 schedule 正文里** + **T2 不许出现** —— 后两条是结构判据:
    「本周X 静默滚成下周X」这条回归真发生时 `list_tasks` 会成功、T2 会进表,
    所以「一条查询结果都没产出」比任何措辞都硬。
  · **正常支**(今天周一~周三)7 个子句:真出表格 + T2 在表里 + **期限列逐字等于工具算好的
    `due_display`** + T1 不在表里(`include_done=False` 真生效)+ 两条否定措辞 + **探针**。
    探针是补问一句「本周一之前有啥任务没干完」(只在周二/周三发,周一那天日历上根本不存在
    「已经过的本周X」),判据 `and` 进 A10 这一条、不新开断言。
    没有它的话,「删掉 `_parse_this_week` 的 raise」这条回归在周一~周三**完全测不出来** ——
    2026-08-10 拿桩后端实测过:注入该回归,无探针版周一/周二/周三全是 25/25 假绿灯,
    有探针版周二/周三变 24/25(周一仍抓不到,那是日历决定的,不是欠账)。
  · 剩下的欠账仍在:`D.a10_due_display` 那条虽然是事实级,但它断的是「模型照抄了工具的输出」,
    还不是「库里/盘上确实如此」。真要按本条的标准改到底,得去核 `list_tasks` 的返回值本身,
    而那条 ToolMessage 在顶层轨迹里拿不到(见 TODO-26)。
  A2 / A7 断的是 `| 任务号 |` 这个表头,而那张表是**模型照 `agents/schedule/prompt.md` 里的
  模板产的、不是工具产的** —— 这两条实际上在赌「模型有没有照抄模板」。
- **反过来更疼:断措辞会漏掉「说得对但没干」。** C2 的判据是 `"GYT-" in whole`,
  而 2026-08-10 真机抓到的那次编造,原话就是「巡检记录出好了,编号 GYT-20260809-153739。」——
  同样含 `GYT-`。**按判据,C2 对那次编造是绿的**,而磁盘上根本没有那份 docx。
  运行期这个洞已经由 `RequireReceiptSource` 堵住(见 CLAUDE.md「中间件分层」),
  但**验收脚本这条判据本身没修**:守卫哪天被人摘掉、或者换 langchain 版本后失灵,C2 不会告诉你。
  这正是 Lane A 那两件结构件要防的病 —— 验收却还站在病的那一侧。
- **该怎么改(D 组就是范本:从 `get_settings()` 取路径、只读 URI 直查、回执说什么不算):**
  ① **C2 优先** —— 从消息里把 `render_inspection_report` 的 ToolMessage 捞出来,
     取信封 `data["report_id"]`,再用 `core/artifacts.py` 的 `resolve` 确认那份 docx 真在盘上。
     这条正对着刚落地的守卫,现在是守卫的盲区。
  ② A1/A3/A4/A5/A6 每一条都有对应的库级事实(库里多了一行 / `due_date` 变了 /
     `status` 从 open 变 done),现在却只在回复文本里找字。D 组已经连着库读了,加断言几乎零成本。
  ③ E2 现在只找 `"50016"` 和「4.0 / 4 米」;`rag.csv` 的 K01 行里有逐条核对过 PDF 的
     `expected_source` / `expected_page`(第 124 页),出处对不对是**可以真断**的。
- **注意别走过头:** 断事实不是要把断措辞全删光。面向工地师傅的人话**本身就是产品的一部分**——
  A8 里那句 `"开小差" not in whole`(乱日期不许甩系统腔)验的就是它,这类该留。
  要改的是那些**拿措辞替代事实**的条目。判据只有一句:「模型只说不做,这条会不会照样绿?」
  会,就是该改的那条 —— A9(不存在的号)这种两头都占的,做法是**两条都断**:
  库里确认没有 T99、也没有被顺手新建一行,同时回复里有如实说明。
- **Depends:** 无。改一条少一条脆断言,不必等齐;C2 建议先改。

---

> 以下四条由 W5 · T3/T4/T6(2026-08-10)的复核产出。前三条都落在真机验收的
> 取证层与派生层上 —— 那两个文件刚重写过,这里记的是**明知道、这一轮刻意没赌**的部分。

## TODO-26 把子 Agent 的工具痕迹捞回验收轨迹
- **What:** `backend/scripts/acceptance_trace.py` 落下来的失败轨迹里,**看不见子 Agent
  调了什么工具、工具返回了什么**。头部只能给出交接计数、可见的工具报错、判据原文和台账快照。
- **为什么(读源码 + Pregel 层验过,HTTP 层没验):** `graph.py:152` 是
  `OUTPUT_MODE = "last_message"`。本机 `backend/.venv/…/langgraph_supervisor/supervisor.py`
  第 77-85 行的 `_process_output`:

      elif output_mode == "last_message":
          if isinstance(messages[-1], ToolMessage):
              messages = messages[-2:]
          else:
              messages = messages[-1:]

  子 Agent 一个回合的最后一条**恒是它自己的纯文本 AIMessage**(工具跑完还得再说句人话),
  所以永远走 `messages[-1:]` —— 带 `tool_calls` 的那条 AI 消息连同它的 ToolMessage 一起被砍。
  supervisor 自己发起的 `transfer_to_*` 留得下来(那是 supervisor 本人的消息,不过这把刀)。
- **不修会怎样:** 三类问题在轨迹里**永远问不出来** —— ①「模型没调工具、凭记忆编回执」;
  ② 工具真返回 `ok:false` 而模型转述成成功(发生在子 Agent 里的那一半,
  `visible_tool_errors` 抓不到,所以那行才叫「本轮**可见的**工具报错」);
  ③ 参数传错(`{"due": "猴年马月"}` 这种)。现在的替代判据是台账快照,
  它只覆盖 A/D 组;E/F 组失败时手上就只有时间线和判据原文。
  **在捞回来之前,谁都不许往头部加「本轮业务工具 × N」这类计数** —— 它在真机上恒为 0,
  是一条恒真的假线索,能把查问题的人往错误方向带一整天(第一版就是这么写的,已删)。
- **从哪下手(两条候选,`langgraph-api 0.12.0` 上逐行核过,都**没在 HTTP 层实测**):**
  1. **首选 `runs/stream` 带 `subgraphs: true`。** `langgraph_api/schema.py:233` 的
     `RunKwargs` TypedDict 里确有 `subgraphs: bool`,`langgraph_api/stream.py:174`
     真在读它(`kwargs.get("subgraphs", False)`),后面 340~540 行一路按它决定要不要
     带命名空间往外吐子图事件。代价:验收现在走的是 `runs/wait`(一次拿回整包 messages),
     换成 stream 要自己拼流、自己判一轮结束,`ask()` 得重写。
  2. **`POST /threads/{id}/state/checkpoint` 带 `subgraphs: true`**(`api/threads.py:291`
     读 `payload.get("subgraphs", False)`)。它回的是**某个 checkpoint 的状态快照**,
     不是整条消息历史,要拿到全貌得自己按 checkpoint 走一遍,比 ① 麻烦。
  ⚠️ **复核报告里写的「`POST /threads/{tid}/history` 带 subgraphs」这条路是错的,别照抄。**
     2026-08-10 读本机安装包原文核过:`get_thread_history`(threads.py:328)与
     `get_thread_history_post`(threads.py:358)**都不读 `subgraphs`**,两个都把
     `"checkpoint_ns": ""` 写死在 config 里。
- **一旦其中一条在真机验通,要一起改的:** `acceptance_trace.py` 的 `_LIMIT_NOTE` 整段、
  「本轮可见的工具报错」里那个「可见」限定、以及 `live_acceptance.py` 头注和收尾那几行
  「头部不会有子 Agent 的工具调用」的说明。
- **Depends:** 无。但它是 TODO-25 那条「断事实」路线走到底的前提(见 TODO-25 末尾)。

## TODO-27 验收轨迹说不清复合断言是哪一半没过
- **What:** 25 条断言里有一多半是复合的 —— A10 报错支 6 个子句、正常支 7 个(含探针那 6 个),
  A6/A7/A8/C3/E2/F2 也都是 `and`/`or` 拼起来的。而 `dump_trace` 落下来的人读版头部只打
  `final()` 和 `sched_text()` **各截 400 字**,人得对着源码里的布尔表达式手工重算一遍。
- **不修会怎样:** schedule 的工作表格三行就超 400 字,判据原文经常是截断的;
  彩排前一小时看到一条 `FAIL A10 …`,要先翻源码数清楚 7 个子句、再去截断的正文里逐个比对。
  这正是 T3 那个模块要消灭的「查不动」,只是换了个形态 —— 从「没有现场」变成「现场读不懂」。
- **从哪下手:** 把 `check()` 的第一个参数从 `bool` 改成 `list[tuple[str, bool]]`
  (子句名 → 真假),`check` 自己 `all()` 出总结论,并把这张表原样传给 `dump_trace`;
  头部直接列 ✓/✗。改动面:`live_acceptance.py` 25 个调用点 + `acceptance_trace.dump_trace`
  多收一个可选参数 + `tests/unit/test_acceptance_trace.py`。
  **别只给复合的那几条改** —— 两种 check 形态并存,下一个人抄的时候必挑简单那种。
- **Depends:** 无。建议和 TODO-25 的「断事实」改造同一批做:那批会让子句更多,不是更少。

## TODO-28 验收取证/派生两层剩下的三条小脆点(都验证过,都不紧急)
- **① `sched_text()` 取的是整条线程的最后一条,不是「本轮」的。**
  `acceptance_trace.py` 的 `sched_text` 扫全量 `msgs` 后返回 `parts[-1]`;而**同一个文件里**
  的 `_round_start()` 就是切本轮的函数,却只给渲染用、没给判据用。
  失败场景:某一轮 schedule 没产出纯文本(反问、或被 `RequireLedgerTool` 的 give_up 顶替),
  `sched_text` 会**退回上一轮的表格**,拿陈年数据判绿。A2/A7/A10 这些断表格的最容易中招。
  改法:抽判据前先 `msgs[_round_start(msgs):]`。**注意这会同时改判据和渲染的口径**,
  改完 25 条断言都要在桩后端上重跑一遍七个星期几,别只跑一天。
- **② `acceptance_dates._load_dates()` 的兜底只认 `ImportError`。**
  它优先走包式 `from gyt.agents.schedule import dates`(为了覆盖率,理由在该文件 docstring),
  失败才按文件路径加载。但 `except ImportError` 挡不住别的异常 —— 而这条 import 会顺带执行
  `gyt/agents/schedule/__init__.py:13` 的 `from langgraph.graph.state import CompiledStateGraph`,
  再往下拖进 guard / tools / base_agent / llm / config,任何一环在换版本后抛
  `AttributeError` / `TypeError` / pydantic 的 `ValidationError`,兜底那条路**根本走不到**。
  2026-08-10 用 `sys.meta_path` 钩子精确模拟过「包在、但 import 抛 AttributeError」:
  异常直接冒到顶,整份验收当场栈崩,而 `dates.py` 就在盘上、按文件路径完全加载得了。
  改法:`except ImportError` → `except Exception`,并在退到文件路径时打一行中文说明
  (**不许静默降级** —— 那会把「gyt 包坏了」这件真事藏起来)。
- **③ `list_tasks` 查空时的 user_msg 说的不是实话。**
  `agents/schedule/tools.py:163`:结果为空就回「台账里现在没有任务。」——
  可事实往往是「台账里有,只是都不在这个 cutoff 之内」。师傅听到这句会以为活丢了。
  这条是 `acceptance_dates._a10_branch` 的注释里挂账的那一条(正常支之所以改问「本周日」
  而不是「本周三」,一半原因就是躲开它)。改法:分开两种空 —— 库里真没有 / 有但都在期限之外,
  后者要把「有 N 条,但都在 X 之后」说出来。**改文案要同步 A10 正常支的判据**。
- **Depends:** 无。三条都是「知道在那儿、暂时不咬人」;②③ 各半小时,①要重跑七天。

## TODO-29 W4 与「Agent 联动」文档里的彩排指引已被 T4 作废
- **What:** 三处文档还在教人「彩排前按当天重算验收脚本里的日期字面量」:
  `docs/W4_跨平台运行方案.md:398`、`docs/W4_跨平台运行方案.html:467`、
  `docs/Agent联动与使用场景分析.html:576`,原话都是
  「19 条断言的日期锚死在 2026-08-08(8月9日周日/8月14日周五/8月10日周一)…彩排前先按当天重算这几行」。
- **为什么现在是反向指导:** W5·T4 之后 `live_acceptance.py` 里
  **一个「会随日期失效」的字面量都没有了** ——
  「今天」从后端台账的 `created_at` 问出来,四句提问的原话和全部期望日期由
  `backend/scripts/acceptance_dates.py` 现算(A1/A4 是固定锚,A3/C3 按候选表顺延避撞车)。
  照着旧指引去「重算这几行」的人会找不到要改的行,更坏的是可能动手把短语写死回去 ——
  那正好把 T4 拆掉的撞车问题原样装回来(每周有两天在验错东西,且屏幕全绿)。
  断言数也从 19 变成了 25。
  ⚠️ **别拿 grep 去验「一个字面量都没有」这句话的字面意思** ——
  `live_acceptance.py` 第 537 行有一处**故意**写死的「T99 改到周五」。
  A9 断的是「不存在的号如实报」,没有任何日期判据,期限说成什么都一样;
  跟着 `D.a3_phrase` 走反而让人以为它和 A3 同源。理由写在它上面第 535-536 行的注释里。
- **怎么处置:** **不要改 W4 和 Agent 联动那两份文档的正文** —— 它们是 2026-08-08 那天的
  历史计划/分析,改了就不再是当时的记录。本条就是那三处的作废声明;真要落笔,
  写进 W5 的方案文档里(md + html 双件套),或者在那三处各加一行「已被 W5·T4 作废,见 TODO-29」
  的旁注,别动原句。
  > **✅ 已办(2026-08-11):三处旁注都加了,原句一律 `<s>`/`~~` 划掉留档、一个字没删。**
  > `W4.md` §10 与 `W4.html` 同位置各补一段作废说明;`Agent联动.html` 除该处外还新增了
  > 一节 **`00 / 校订`**(目录里也加了入口),把这份文档里全部已过期的现状陈述集中列了出来
  > —— 不止日期这一条,还有「knowledge/cad 未实现」「15/22 = 当前满上限」
  > 「等两位落地后能跑的六个」「`rm -f backend/data/gyt.sqlite3`」这几条,
  > 后两条比日期那条更咬人(一条劝人别走已修通的路,一条是空操作型清库)。
- **Depends:** 无。**彩排前必须有人读到这条**,否则那一小时会白花在一个不存在的 bug 上。

## TODO-30 状态陈述审计(2026-08-11)——已订正的与留给人拍板的
- **What:** 全仓扫了一遍「用现在时描述当前状态、而状态已经变了」的陈述。
  这类句子过期时**不会有任何报错**,2026-08-10 已经因此出过一次事(19 断言 / 清错库)。
  本轮订正的清单见各文件的旁注,不在这里重抄。下面只留**没动、要人拍板**的几条。
- **① 安全集难例只有 1 张,建议配比说 5 张。** `eval/README.md` 的「建议配比(共 30 张):
  违规 15 · 合规 7 · 难例 5 · 非工地 3」是**设计目标**不是现状陈述,按铁律不该我改。
  实况(按 `type` 列):违规 16 · 合规 8 · **难例 1** · 非工地 5。
  已在 README 里把实况标出来了。**要补难例、还是把建议改成现状,请人定。**
  连带:`validate_safety_row` 根本不校验 `type` 列,这一栏漂了不会有任何测试变红(见 TODO-14 第 3 条)。
- **② `ROUTING_AGENTS` 里的 `report` 是合法但永远判不对的取值。**
  `scorers.py` 的 `ROUTING_AGENTS` 与 `eval/README.md` 都把 `report` 列为合法 `expected_agent`,
  而 `AGENT_REGISTRY` 里没有 report、`transfer_to_report` 这条路不存在。
  **今天零影响**(routing.csv 22 行没有一行填 report),但谁哪天填了,
  `validate_routing_row` 会放行、那行永远判成「该给 report 却谁都没派」——
  正是这道校验当初要防的失败模式。**是「留着以备将来独立挂载」还是摘掉,请人定。**
- **③ 文档 md + html 双件套有五个反例。** `CLAUDE.md` 写「方案类交付物是 md + html 双件套」,
  而 `docs/` 下:`T1_骨架搭建计划` / `W2_执行计划` / `Agent联动与使用场景分析` / `标注台`
  只有 html,`工友通_AI_Agent_MVP_核心设计` 只有 md。
  读起来像「今后的约定」而不是「当前的盘点」,早期件不齐可能是有意的。
  **改措辞(「W3 起的方案类…」)还是补件,请人定。**
- **④ `docs/工友通_AI_Agent_MVP_完整技术方案.md` 指向一个不存在的文件。**
  原文「演示脚本(`docs/demo-script.md`,W4 产出)」,而 `docs/` 下没有这个文件;
  实际的演示脚本长在 `docs/Agent联动与使用场景分析.html` 的 `09 / 演示` 节。
  整份文档是 2026-08-06 的计划、「W4 产出」是未来时,严格说不算过期陈述,所以没动 ——
  但找演示脚本的人会照它去找一个空路径。
- **⑤ `T1_骨架搭建计划.html` 说「日常开发不必走 Docker」**,与 W4 定案(两人统一走容器)相反。
  那是 T1(2026-08-06)骨架计划的正文,属历史;结论已改写进 `backend/README.md` 的「1.5 先选路线」。
  **要不要在那句上加一行「已被 W4 改写」的旁注,请人定。**
- **⑥ Makefile 里两处结构问题(不是状态漂移,所以没动):**
  `e2e` 目标的注释说「与 up 目标同样的两条前置」,实际用的是自己抄的一份弱版
  (裸 `mkdir` + 自写 `test -f`,而 up 用的是 `PREPARE_DATA_DIR` / `CHECK_ENV_KEYS`,
  含 mkdir 失败拦截和 Linux chown 提示)。文件里自己写着「写成变量而不是每个目标各抄一份:
  文案漂移过一次」—— e2e 就是那第二份。另:文件头约定「后端命令一律经 uv 执行」,
  而 `serve-artifacts` 用的是系统 `python3 -m http.server`。
- **Depends:** 无。六条都不咬人,但①②是「今天零影响、改动时才咬」的那种,值得在动数据集前先定。

## TODO-31 Supervisor 遇到「一件事要两位同事」会原地打转,而且它就是 B6 那条 P0

- **What:** 用户问「这张图纸合不合规」时,supervisor 把犹豫**写进了正文**,同一个两难重述七八遍
  (「让我再想想」「让我重新理解分工」「我决定派给 knowledge」→ 又推翻),最后要么熔断,
  要么给一句假回复。观感极差,而且**这不是观感问题**。

- **Why(三条,2026-08-11 全部在代码里核过):**
  1. **它没有放盘算的地方。** `config.py:187` 的 `disable_thinking_for_text: bool = True`
     → `llm.py:334` 给请求带 `extra_body = {"thinking": {"type": "disabled"}}`,**思考通道是关的**;
     而 `graph.py` 的 supervisor 提示词规则 5 又写着「你说出口的每一句话都是直接讲给用户听的,
     不是自言自语的盘算」。**没有思考通道 + 禁止出声盘算 = 它只能把盘算写进正文。**
     规则拦不住一个没有别处可去的过程。
  2. **`temperature=0` 把「犹豫」变成「死循环」。** `config.py:204` 是 0(执行类任务要确定性,
     那个取舍本身是对的)。但温度 0 每次取最高概率续写:推理走到「cad 不答规范 / knowledge 不看图纸」
     这个死结时,没有高概率的**决断性**续写,于是最高概率的下一步就是重述两难 ——
     重述完上下文形状几乎没变,又产生同样的续写。**这是确定性的打转**,温度高反而会随机跳出去。
  3. **提示词缺一条判据。** 规则 2 管的是「一句话里有**多件事**」(拆开按顺序派);
     而「图纸合不合规」是**一件事需要两位同事**(cad 拿事实 + knowledge 拿标准)。
     模型正确地发现规则 2 套不上,然后无规则可依。

- **它就是 B6。** `docs/Agent联动与使用场景分析.html` 场景 B6(项目经理问「你们外架间距合规吗」)
  被文档自己标为「观赏性天花板,风险也是天花板」。2026-08-11 真跑抓到:supervisor 只发了
  `transfer_to_cad`,收尾那条消息**没有任何 tool_calls**,而它的最后一句是
  「所以规范那条我**转给查规范的同事了**」—— 说了没做,然后结束。项目经理会一直等一个不来的答复。
  同一个根因的两种表现:话多的那次是打转,话少的那次是假承诺。

- **怎么修(顺序不能反):**
  1. **补判据(治根因,顺带修 B6)。** supervisor 提示词加「一件事需要两位同事」的处理法:
     先派能拿**事实**的那位,结果回来再派拿**标准**的,最后自己汇总。
     改 supervisor 提示词**不碰视觉缓存**(那个键是 `vision_prompt.md` + `prompt_version` + 视觉模型),
     代价只有几条便宜的文本缓存。
  2. **结构件兜底。** 本仓三次栽在「用提示词管住模型行为」上,这是第四次。
     supervisor 嘴上说「转给同事了」手上没调工具 —— 该有个守卫抓住,
     与 `core/require_tool.py` 的 `RequireReceiptSource` 同族:**输出里承诺了的动作,
     必须在本回合真的发生过**。判据待设计(「转给/派给 + 同事名」这类措辞 × 本回合 transfer 记录)。
  3. **(观感,可选)把盘算搬到单独通道 + 前端折叠。** supervisor 单独开思考,
     推理走 `additional_kwargs`(`langchain_openai` 源码里 18 处引用 `reasoning`,
     **但 DeepSeek 的字段名是 `reasoning_content`,会不会落进这个字段我没端到端验过**),
     `ai.tsx` 覆盖件折成一行。

- **⚠️ 只做第 3 条 = 把「看得见的坏」变成「看不见的坏」。** Claude Code 的思考轨迹好看,
  是因为它的思考**在推进**;这段轨迹的毛病是原地转。折起来的结果是用户盯着转圈指示器等二十秒,
  然后拿到 B6 那种假回复。**先做 1、2,再考虑 3。**

- **Depends:** 无。1 是改提示词,便宜;2 要设计判据 + 单测,参考 `core/require_tool.py` 顶部
  那段「两种判据,别混用」的推演 —— 那里记着 2026-08-10 给 report 套错判据的完整教训。

## TODO-32 熔断之后用户什么都看不到 —— B2 实测撞上了文档自己预测的那条 P0

- **What:** `docs/Agent联动与使用场景分析.html` 的场景 **B2**(技术员早上八点交底:
  「打开地下室那张图,看看柱距多大」→「记一条:明天放线复核 3-5 轴柱距」,链路 cad → schedule)
  真跑了一遍,三件事同时成立:
  1. 这条提问跑进 cad Agent 后,抛 `GraphRecursionError: Recursion limit of 8`;
  2. 客户端(浏览器)收到的是**裸的英文 JSON 报错**;
  3. 而**界面上什么都不显示** —— 用户这一侧的完整体验是「发了消息,没反应」。

- **这是「文档预测到了、代码没防住」的一条。** 同一份文档 §07 已经把
  **「P0 · 熔断无用户可见兜底」**列在「仍然敞着的口子」里(依据是它 §06 的发现 2 + 发现 3:
  三跳到顶、四跳熔断,而熔断后端零处理)。**那不是一条虚的风险预测,B2 一跑就撞上了。**

- **Why 是 P0(两条,第二条比第一条重):**
  1. 裸英文 JSON 直接违反 CLAUDE.md 的语言约定 —— 面向用户的字符串必须是工地师傅看得懂的人话,
     不许出现堆栈、类名、内部路径。
  2. **报错甚至没到用户眼前。** 屏幕上没有任何东西变化,人无从判断是自己网不好、
     还是话问错了、还是系统坏了,唯一的动作只能是反复重发。
     演示席上这一幕比看到一句看不懂的英文更糟:英文至少还是个「出事了」的信号。

- **⚠️ 不是 TODO-31,别合并。** TODO-31 记的是 supervisor 遇到「一件事要两位同事」时
  反复派活 / 复读(B6 那条链),病灶在**熔断之前**;这条记的是**熔断之后的错误呈现** ——
  不管是什么原因把步数烧到 8,呈现这一段都得有人兜。**两者可以独立修,谁先修都不挡对方。**

- **⚠️ 也别以为 `backend/auth.py` 已经把这个盖住了。** 那里的 `_reject_oversized_recursion`
  拦的是**客户端自己传上来的**超大 `recursion_limit`(上限是 `config.py:215` 的
  `max_client_recursion_limit=16`),防的是有人把步数调高去烧 API 额度 —— **和这条是两回事**。
  这条撞上的是编译期钉的那个值:`config.py:208` 的 `supervisor_recursion_limit`(默认 8),
  由 `graph.py:470-472` 的 `.with_config()` 钉到图上。**auth.py 那件做得再对,B2 该炸还是炸。**

- **修之前先查清一个对不上的地方(我只核到这儿,再往下没查):**
  覆盖件 `scripts/frontend-overrides/thread-index.tsx:169` 里挂在 `stream.error` 上的
  `toast.error("An error occurred. Please try again.")` **是在的**(`setup-frontend.sh:386`
  确实把它盖到 `src/components/thread/index.tsx`),照这段代码本该弹一个英文 toast ——
  而实测界面上什么都没有。**两者对不上,原因没查。**
  落点该在前端还是后端,取决于先把这个查清楚。
  顺带:文档 §06 发现 3 写的「这个 toast 在**上游文件**里,不是三个覆盖件之一」**今天不成立** ——
  `thread-index.tsx` 现在就是覆盖件之一(CLAUDE.md 把它列在 DXF 上传四件里)。

- **和 TODO-9 的关系:** TODO-9 管的是 Agent 路径上的错误文案中文化
  (在图的出口统一过一遍 `_classify` / `_user_msg`)。**即使 TODO-9 做完,也只解决「英文」那一半** ——
  上面第 3 点「报错到不到用户眼前」不在它的范围里。

- **Depends:** 无。

## TODO-33 线上 VPS 跑的后端镜像比 main 少一个提交(cad 竞态修复没上线)

- **What:** `4cb2809`(cad 索引临时文件名改唯一,修同回合两个工具并发抢 tmp 的竞态)
  **已进 main、已推,但 velactora.com 上跑的 `gyt-backend:vps` 里没有它。**
  线上那份是 `be1016e7aecc`,构建于 2026-08-11 早些时候。

- **为什么没上线(不是忘了,是当时判断不该赌):**
  重建镜像时两次都撞 `no space left on device`。根因是我自己造成的 ——
  为了给第一次构建腾地方跑了 `docker builder prune -af`,把**依赖层缓存也一起清了**,
  于是本该几十秒的增量重建变成全量(155 个包重下 + 2.2GB BGE-M3 权重重下),
  而 30GB 的盘要同时装下新旧两份 6.37GB 镜像的解开态,不够。
  当时站点是**已验证可用**的状态,拿它去赌一个非紧急修复不划算,所以停手。

- **⚠️ 停手时留下过一个危险中间态,已经处理掉了,但值得记住:**
  构建失败在「解开层」这一步,而**镜像名已经打上去了** —— `gyt-backend:vps`
  当时指向一个**没解开完**的新镜像,跑着的容器用的却是旧的。
  **机器一重启,compose 就会去用那个坏镜像起后端。**
  处理:`docker tag gyt-backend:known-good gyt-backend:vps` 指回去 + `docker image prune -f`,
  然后**真的 `--force-recreate` 了一次 backend 容器**确认它能从当前 `:vps` 起来(healthy)。
  教训:构建失败之后**必须查一眼 `docker images` 里 tag 指到哪儿了**,
  「构建失败」不等于「什么都没变」。

- **怎么补上(需要先腾地方):**
  ```bash
  # 在 VPS 上。先确认有 ≥15GB 空闲,否则先删旧镜像再建(那样构建期间后端会下线几分钟)
  df -h /
  cd /opt/gyt && docker compose -f docker-compose.yml -f docker-compose.vps.yml build backend
  docker compose -f docker-compose.yml -f docker-compose.vps.yml up -d backend
  # 建完再打一次回滚标签
  docker tag gyt-backend:vps gyt-backend:known-good
  ```
  **别在重建前跑 `docker builder prune -af`** —— 那正是这次踩的坑:
  它清掉的依赖层缓存,恰恰是让重建变便宜的东西。要腾地方优先删
  `gyt-frontend:known-good` 这类**不同摘要的旧镜像**。

- **在此之前线上的实际影响:** cad 场景里如果一个回合触发两个 cad 工具并发,
  其中一个会抛 `FileNotFoundError` 而失败(另一个成功)。不破坏数据,
  同样的提问重跑一遍通常就好了 —— 但演示时撞上会很难看。

- **Depends:** 无。

## TODO-34 英雄链偶尔不出 docx,而且给用户的话是「去找工友通那边报给管理员」

- **What:** 2026-08-11 在 velactora.com 上跑演示场景 A5(项目经理:「T5 那个临边防护我拍了张,
  出份巡检记录」+ `photo_03.jpg`)时:
  - 路由**是对的** —— `transfer_to_inspection`(safety→report 的英雄链);
  - 照片正常识别,而且识别结果与 `eval/datasets/safety.csv` 的标注**一字不差**
    (`临边无防护;未穿反光衣`);
  - **但产物目录里只落了照片,没有 docx**(该轮前后 docx 总数没变);
  - 用户最终看到的原话是:

    > 我这边只能看照片,**出不了正式留档的巡检记录文档**。要出记录得走系统流程,
    > 我这边没有生成文档的工具。您要出正式记录的话,得找**工友通**那边,
    > 把照片编号(f669907a…)报给管理员,让他们出巡检记录。

- **两层问题,第二层比第一层严重:**
  1. 该出的文档没出。
  2. **它把自己当成了别人。**「找工友通那边」「报给管理员」—— 用户正在用的就是工友通,
     没有什么管理员。一个让用户去找一个不存在的人的回答,比直接说「出不来」更坏。
     照片编号(32 位 hex)也不该出现在给工友看的文案里。

- **说话的像是 safety 而不是 report**:「我这边没有生成文档的工具」正是 safety 的处境
  —— `render_inspection_report` 在 report 手里。所以现象更像**第二跳没跑到 / 或它的输出
  没成为最后一条消息**,而 safety 那句「我出不了」被当成了最终答复。
  **但这只是根据文案推的,没查证。** 修之前先把这一跳的消息序列打出来看。

- **和已有条目的边界:**
  - 不是 TODO-31(那条是 supervisor 反复派活 / 一件事要两位同事);
  - 不是 TODO-32(那条是熔断后裸英文 JSON + 界面无提示);
  - **和 `core/require_tool.py` 的 `RequireReceiptSource` 也不是一回事** ——
    那件防的是「报了个编号但文件不存在」,而这次是「老老实实说出不来,但说错了对象」。
    守卫没被触发是**对的**。

- **同一批里 A4 也有相关表现**(「我已经转给负责出巡检记录的同事了……稍等,报告出来我马上
  告诉你」然后回合就结束了),那个更像 TODO-31 的「承诺了却没做」。两者可能同源,没查。

- **Depends:** 无。

---

> 下面四条来自 2026-08-14 的**打卡功能方案评审**(`docs/W7_打卡Agent_技术方案.md`)。
> 四条**都是负责人在看过取舍后明确接受的残余风险**,不是遗漏 —— 记在这儿是为了
> 别在交付给客户时才被问到。

## TODO-35 打卡的「谁」是 localStorage 里的一个字符串,不是认证身份

- **What:** W7 方案的 D2/D4 定案:线上登录是**单一共享口令、没有用户名**
  (`scripts/serve_login.py` 的 `GYT_LOGIN_HASH` 是单个 hash),所以打卡记录里的
  `worker_name` 来自**前端 localStorage**,随请求带上。

- **Why 这是个洞:** 它回答的是「谁在用这台手机」,不是「谁通过了认证」。
  任何人改一下 localStorage 就能用别人的名字打卡,而水印上会原样画出那个名字。
  **凭证看起来很正式,但它证明不了人。**

- **还有两个连带问题**(方案 §7 已列,别当成三件事去修):
  1. `worker_name` 不是身份主键 —— **同名的两个人会被合并**,同一个人写简体一次写繁体
     一次会被**拆成两个人**。没有 `worker_id`,所有按人查询都不可靠。
  2. 「今天谁到了」这类查询也是按姓名字符串聚合的,继承同一个缺陷。

- **怎么修:** 归 TODO-3(用户体系)。真做的时候,W7 已经留好接缝 ——
  **只需把姓名的注入来源从 localStorage 换成会话,后端一行不用改**(方案 D4 的设计意图)。
  同时要加 `worker_id` 列并回填历史数据。

- **在此之前必须做的**(**已在 W7 范围内**):`agents/attendance/prompt.md` 与登录页的
  PICS 都要**明说这不是实名制考勤** —— 否则工友会拿它当工资凭据,而香港的法定考勤
  走的是《建造業工人註冊條例》的读证/DAR 路径,和这个东西无关。

- **Depends on:** TODO-3。

## TODO-36 打卡台账没有权限模型 —— 任何登录者能查所有人

- **What:** 共享口令 + 没有 `project_id` 隔离(建表即预留、写入恒 NULL),
  于是**任何登录进来的人都能查所有工人的姓名、打卡时间、地盤**。

- **Why:** 香港 PDPO 下,个人资料的访问要有必要性和适度性。「班组长能看到全组」是
  合理的业务需求,但现在**每一个拿到那串共享口令的人都是班组长**。

- **W7 已经削弱但没根治:** 方案 §3.10 定了最小化约定 —— **Agent 的查询工具**返回值
  **不含经纬度、不含 `artifact_id`**。这把 LLM 那条链的泄露面从「坐标 + 自拍」
  缩到「谁、什么时候、哪个地盤」,但没解决「谁有权查」。

- **⚠️ v4 把口径改了一半,别照着旧版理解:** 方案 v3 曾设想「凭证图按 `receipt_no` 单独取」,
  v4 已废除那条路 —— **`artifact_id` 现在会出现在 `POST /checkin` 与 `GET /checkin/recent`
  的 HTTP 响应里**(理由:`<img>` 送不了自定义请求头,只能走既有的
  `${ARTIFACT_BASE}/by-id/<artifact_id>` 出口,见 W7 方案 §1.3)。
  **所以「最小化」只约束喂给 LLM 的东西,不约束本人的直连接口** ——
  而在共享口令下,「本人」和「任何登录者」是同一回事:**拿到 `artifact_id` 就能拉到那张自拍**。
  这不是 v4 新开的洞(artifacts 静态服务本来就只靠 Caddy 的登录 Cookie 挡),
  但**泄露面确实变大了**:以前要先知道 `receipt_no` 再换,现在响应里直接给。

- **怎么修:** 同样归 TODO-3。有了用户体系之后:①按 `project_id` 隔离;
  ②区分「查自己」与「查全组」两种权限。

- **Depends on:** TODO-3。**Codex 复审时把这条定性为「查询功能的上线前依赖」而非
  「以后优化」** —— 记下这个不同意见,交付给真实客户前要重新掂量。

## TODO-37 PDPO 只做了登录页声明,雇员监察的其余要求全欠

- **What:** W7 的 D13 定案:PICS 放在**登录页底部静态文字、不拦截、不存同意记录**。

- **已落地(2026-08-15,T12):** `scripts/login-page.html` 底部 `<details class="pics">`
  六项齐:用途 / 是否自愿 / 不提供后果 / 接收方类别 / 查阅更正 / 负责人。
  ⚠️ **负责人一项是占位措辞**(「本系统管理员,联系方式见班组公告」)——
  真实姓名/职衔/联系地址未提供,交付真实客户前必须补实(页面注释里也标了)。

- **仍欠的**(PCPD 对雇员监察的要求不止「页面上有一段字」):
  - **必要性与适度性**论证 —— 为什么非要自拍 + 精确坐标,有没有侵扰更小的替代;
  - **监察政策**文本(告诉雇员会被监察什么、多久、谁能看);
  - **删除流程** —— 工友要求删掉自己的打卡照,现在没有任何流程
    (页面上承诺了「可查阅更正」,但流程实体只有「找管理员」四个字);
  - **处理者保障** —— 谁在替我们处理这些资料、合同里怎么约定;
  - **同意记录** —— D13 选的是不存(负责人拍板,风险已当面陈述过一次)。

- **⚠️ 跨境传输的准确说法(2026-08-15 订正,别再抄旧版):**
  D15 把打卡**写入**改成直连接口后,**自拍与精确坐标**不再进模型上下文 ——
  但「从根上解掉跨境」是 v2 的错误说法:**查询链的问题与工具结果仍带着
  姓名、时间、地盤名发给 DeepSeek(内地服务商)**。PICS 第 4 项就是按这个
  现实写的(两句都不许说过头:名字确实出境,照片和坐标确实不出)。
  另:PDPO 第 33 条(跨境转移)至今未生效,现行硬要求是**接收方类别披露**。

- **怎么修:** 交付给真实客户前,照 CIC「前線人員安全表現紀錄計劃」的私隱政策聲明
  出一份完整的。参照: https://safetyrecording.cic.hk/content/dam/safetyrecording/resources/pps-tc.pdf

- **Depends on:** 无技术依赖,是文书工作 + 一个删除流程。

## TODO-38 夜班跨午夜时 `work_date` 的业务语义未定义

- **What:** W7 的 `attendance` 表把 `work_date` 定义为 `checked_at` 的**日历日**。
  夜班工友 23:50 打一次、次日 00:10 再打一次,会被算成**两天**。

- **Why 现在不修:** 定义「工作日边界」(比如以早上 6 点为界)需要知道客户的实际班次安排,
  而且一旦改了定义,历史数据的口径就变了。**在没有真实客户排班信息之前定这个是瞎猜。**

- **发现路径:** Codex 复审 W7 方案时指出。当时的原话是「夜班跨午夜时
  `work_date=日历日` 也未定义业务语义」。

- **修的时候要注意:** `work_date` 是**落库的派生列**(为了走索引),不是每次查询现算的。
  改定义就要**回填全表**,而且 `COUNT(DISTINCT work_date)` 的结果会变 ——
  也就是说「张三上个月来了几天」这个数字会在升级后**改变**,得提前跟客户打招呼。

- **Depends on:** 客户的实际班次安排。

## TODO-39 打卡与工地的关联:**用定位反查,不给工友加下拉**

- **What:** 2026-08-15 两条 W7 分支合流后,同一个系统里有两套「工地」:

  | | 队友那条线(cad / knowledge / 资料) | 打卡这条线 |
  |---|---|---|
  | 工地是什么 | `projects` 表里的一行,有 `id` | `attendance.site_name`,**工友手打的自由文本** |
  | 怎么选 | 顶栏下拉,记 localStorage,submit 时经 `config.configurable.gyt_project_id` 注入 | 打卡面板里一个输入框 |
  | 数据约束 | `drawings.project_id TEXT NOT NULL REFERENCES projects(id)` | `attendance.project_id` **可空、无外键、写入恒 NULL** |
  | 查询作用域 | 选了工地就只查那个工地的图纸/规范 | 全组全量,不分工地 |

- **会咬人的场景:** 工友在 A 工地打了卡,班组长顶栏选着 B 工地问「今天谁到了」,
  **答出来的是全部人** —— 不报错、不提示,看着就像数据串了。

- **方向已定(2026-08-15 负责人拍板):打卡侧不加项目下拉,用「打卡时的坐标」反查工地。**

  理由是产品性的,不是技术性的:**工地上少打一个字,就少一个不用它的理由。**
  而且「手打的地盤名对不上建档名」这个问题从根上没了 —— 不再有第二套真相。
  前提是 `projects` 补上坐标:**id + name + 坐标(+ 图纸)凑齐,四条线才串得起来。**

- **前置(在队友那侧):`projects` 表现在只有 `id / name / code / created_at`,没有坐标。**
  要加 `lat` / `lon` / 半径(或用图纸的大地坐标推)。这一步不做,下面全都无从谈起。

- **怎么落(打卡这侧):**
  1. `POST /checkin` 已经收了坐标(`X-GYT-Geo`,六态),**不需要新增任何 header** ——
     这正是这个方案便宜的地方;
  2. **在写入时解析并落库** `project_id`,同时**保留原始坐标**。
     ⚠️ **不许在查询时现算** —— 那样改一次项目坐标,历史上所有考勤的归属都会跟着变,
     「上个月张三在 A 工地干了几天」这个数字会自己动。写时定死、原始坐标留着供审计重算。
  3. 查询侧顺带收口:`agents/attendance/tools.py` 两个工具加注入参
     `*, config: RunnableConfig`(⚠️ 注解必须**恰好**是 `RunnableConfig`,写成
     `RunnableConfig | None` 会让 langchain 判定失败、config 永远拿不到 ——
     `core/run_context.py` 头注写明了这个坑),用现成的 `project_from_config()`,别另造键。

- **⚠️ 两处会咬人的,都是实测出来的,别当顾虑:**

  1. **拿不到坐标的打卡不是边缘情况。** 本机真机试了三次就有一次 `timeout`
     (冷启动首次定位要 5.3 秒,见 W7 方案 §12.4)。工地信号只会更差,
     还有工友直接拒权限的。所以 **`site_name` 不能删** ——
     它是「没有坐标时」唯一的线索,得留着当兜底与人工核对的依据。
     六态里 `ok` 之外的五种,`project_id` 只能是 NULL,查询侧要能如实说
     「有 N 条没记到工地」,**不许猜**。
  2. **精度撑不住相邻地盘。** 实测 ±35 米(WiFi 定位;手机 GPS 户外会好些、
     楼里更差,**这个数我们没量过,别照抄**)。香港市区两个地盘挨着是常事,
     半径一定要可配,而且**撞车时记「疑似 A / B」而不是挑一个** ——
     挑一个的错法是静默的,而考勤是要发给人看的账。

- **⚠️ 别顺手加外键。** `attendance.project_id` 现在没有 `REFERENCES projects(id)`,
  是对的:加了就要求「工地先建档才能打卡」,而打卡要的是「人到了就能记上」。
  坐标反查天然也是这个姿势 —— 匹配不上就留 NULL,不是拒绝打卡。

- **历史数据:** 已有的行 `project_id` 是 NULL,但**坐标还在**(`geo_status='ok'` 那些)。
  所以等 `projects` 有了坐标,老数据是**能回填的** —— 这比原来「手打名字没法自动映射」
  的处境好得多。没坐标的那些(timeout / denied)只能留空或人工对。

- **延伸(用户提的,单独一条别混进来):** 项目有了坐标之后,图纸也能挂到地理位置上
  —— 「站在这个位置对应图纸上哪儿」。这是另一个量级的事(要处理图纸的大地坐标系,
  `agents/cad/` 里已经踩过「大地坐标真图渲染出来是空白」),要做请单开 TODO。

- **Depends on:** `projects` 表加坐标(队友那侧)。与 TODO-36(权限模型)有关联但
  不互为前提 —— 那条管「谁能查」,这条管「查哪个工地」。

---

## TODO-40 30GB 盘装不下一次后端重建 —— 且失败会把镜像记录 GC 掉,容器从此停不得

- **What:** 这台 VPS 30GB 盘,后端镜像 6.37GB。**一次全量重建的峰值约 13.2GB**
  (BuildKit 缓存 6.8GB + 解包进 containerd 6.37GB),而扣掉运行中容器占的层之后,
  可用只有 11GB 左右。**结论:这台机器上,后端镜像的全量重建从来就不成立** ——
  以前能成,全靠增量缓存;缓存一没,必炸。
- **2026-08-16 实测的完整事故链**(比 TODO-33 记的严重,那条只到「半截镜像」):
  1. `Build Cache 0B`(TODO-33 那次 `docker builder prune -af` 的后遗症)→ 全量重建;
  2. 走到 `#27 unpacking` 时 `no space left on device`,构建失败;
  3. **`gyt-backend:vps` 这个 tag 已经被打到那个 1.95GB 的半截镜像上**;
  4. 🔴 **Docker 29 用 containerd 存储:tag 一移走,没有名字引用的旧镜像记录会被 GC。**
     等我去 `docker tag <旧ID> gyt-backend:vps` 时报 `No such image`;
     `docker commit <运行中的容器>` 报 `content digest not found`(config blob 也没了)。
  5. 于是进入最坏状态:**容器靠自己的快照健康运行,但既不能打 tag、不能固化、也不能重启。**
     停一下就再也起不来,而 `docker ps` 一切正常、站点一切正常,**外部完全看不出来**。
- **⚠️ 附带的连锁:`docker compose up -d <任一服务>` 会顺手重建 backend**(它发现镜像没了),
  报 `No such image` 后**整条命令中止**,连你本来要动的那个服务也没动成。
  必须加 `--no-deps` 才能隔离开。这一条在镜像缺失期间是**每一条 compose 命令的地雷**。
- **⚠️ `docker buildx prune -f` 清的是全部缓存,不是 `docker system df` 报的那个
  「可回收」数。** 我按「可回收 1.633GB」去理解,实际清掉 7.833GB —— 那里面 6.2GB
  正是刚建好、能让重跑秒过的层。
- **⚠️ `DOCKER_BUILDKIT=0` 这条省磁盘的路走不通:** 经典构建器不支持
  `RUN --mount=type=cache`,而 `backend/Dockerfile` 用了它。
- **当时怎么脱的困:** 唯一能腾出空间的办法是**释放运行中容器占的层** ——
  `docker rmi -f` 只去掉 tag、一个字节都不释放,必须**删掉容器本身**。
  最后是删掉前端容器(3.1GB)换到 14.2GB 才建成,代价是界面停了约 25 分钟。
- **下次要怎么做(按优先级):**
  1. **别让缓存断。** 缓存在,重建是增量的、几十秒、几百 MB;缓存一没就是这条事故链的起点。
     `docker builder prune -af` / `buildx prune -af` 在这台机器上等于「预约一次事故」。
  2. **重建前先量峰值**:`可用空间 ≥ 镜像大小 × 2 + 1G`。不够就别开始,先删容器腾层。
  3. **构建前先把当前镜像存一份**:`docker save gyt-backend:vps | gzip > /root/backend-known-good.tgz`
     (约 3-4GB,盘紧时可存到别处)。有它就不会出现「唯一的好镜像被 GC 掉」这一幕。
     ⚠️ **`docker tag <旧ID>` 不算备份** —— 旧 ID 在 tag 移走后可能已经不存在了。
  4. **根治是扩盘。** 30GB 对「6.37GB 镜像 + 需要双份空间的构建」本来就不够,
     现在是靠人工腾挪硬撑。
- **Depends:** 无。下一次改后端代码就会再撞上。

## ~~TODO-41 巡检记录编号用的是**宿主时区**,不是业务时区权威~~ ✅ 已收口(2026-08-16,W9 S2)
- **怎么收的:** 生成端从 `agents/report/tools.py` 里裸拼 `strftime` 搬进了
  `core/doc_no.py` 的 `new_report_no()`,时间快照走 `attendance/receipt.py` 的
  `make_snapshot()`(Asia/Hong_Kong 权威)。**编号格式一个字节没动**
  (`GYT-%Y%m%d-%H%M%S`),`REPORT_RECEIPT_PATTERN` 仍匹配得上 —— 实跑验证过,
  且六种新监理文书编号一条都不被它误匹配。
- **测试怎么钉的:** `test_doc_no.py` 里 monkeypatch 掉 `doc_no.make_snapshot` 后
  编号必须跟着变。只断「日期是今天」的话,在 UTC+8 的本机永远绿 —— 那正是这条
  TODO 当初能藏住的原因。
- **顺带修的两处失效指针:** `core/require_tool.py` 与 `agents/report/__init__.py`
  都写着「`tools.py:151` 的 strftime」,生成端搬走后这两行就指错了。
- 以下为原始记录,留档不改:

## TODO-41(原始记录)巡检记录编号用的是**宿主时区**,不是业务时区权威
- **What:** `agents/report/tools.py:151` 生成记录编号、`:85` 填「生成时间」,两处都是
  `datetime.now(UTC).astimezone()` —— 不带参数的 `.astimezone()` 取的是**宿主时区**。
  而 CLAUDE.md 的同源清单里写得很清楚:业务时区 `Asia/Hong_Kong`,
  **唯一时间权威 = `attendance/receipt.py`**(`HK = ZoneInfo("Asia/Hong_Kong")`),
  「不许靠宿主 `TZ` —— 容器是 Shanghai,数值一致是巧合」。
- **为什么今天看不出来:** 线上容器 `TZ=Asia/Shanghai`,与香港同为 UTC+8,
  两种写法算出来的日期**一模一样**。所以没有任何测试会红,也没有任何人会察觉。
- **什么时候会爆:** 服务器换到非 +8 时区(或有人在 compose 里改了 `TZ`)的那一刻,
  巡检记录编号里的日期与现场日期差一天。**编号里的日期不带偏移量**,
  不像 `_now_iso()` 的 ISO 串还能靠偏移量还原 —— 错了就是真错了,而且是印在留档文档上的。
- **顺带:** `db/tasks.py:82` 与 `db/projects.py:125` 的 `_now_iso()` 也是同一个写法,
  但那两处**不是 bug** —— `isoformat` 带偏移量,时刻无歧义。只有**不带偏移的日期**才受影响。
  别顺手一起改,那会制造无谓的 diff。
- **怎么改(等 W9 的 T4 落地之后,就是换一行):**
  W9 的 D7 定案会建一个共用文书编号生成器,时间快照直接取 `receipt.py` 的 HK 权威。
  建好之后把 report 这两处改成调它即可。**当前部署上数值零差异,改动安全,现有测试不会红。**
- **为什么现在不改:** W9 评审(2026-08-16)刻意不扩范围 —— 它动的是已上线英雄链的产物编号逻辑,
  而英雄链是演示主链。等 T4 有了正确对照件再一起换,比现在单独改一处安全。
- **Depends:** W9 的 T4(共用文书编号生成器)。
- **来源:** `/plan-eng-review` 审 `docs/W9_监理业务闭环_方案.md` 时翻出(议题 6 / D7)。

## TODO-42 W9 监理链落地后的残余欠账(2026-08-16,`feat/w9-supervision` 12 个提交)
八条泳道全部落地、全量 1669 passed、覆盖率 92.80%、ruff 全绿。以下是**明知留下**的,
不是没做完 —— 每条都写了为什么现在不做。

- ~~**🔴 routing 评测必须整套重跑**~~ ✅ **已跑(2026-08-16 当天)**:
  **33/33 = 100%,门槛 90%,PASS**。两行刻意的对抗行(R32「记一下:明天上午整改临边防护」、
  R33「复检钢筋那条任务改到周五」,都标 `schedule`)**都没被 supervision 钓走** ——
  R32 的注释原话是「被钓走 = 真机验收 C3 翻车的预演」,它绿了说明 C3 那句改的措辞方向是对的。
  留档已写进 CLAUDE.md 取代作废的 26/26。⚠️ 判分噪声:routing 判的是首跳 Agent 名、
  不过外部 LLM 裁决,不像 safety 有 ±1 行固有噪声 —— 这个数比 safety 的可复现。
- ~~**🔴 后端没有「否决」端点。**~~ ✅ **2026-08-16 W10 已补** `POST /supervision/reject`,
  前端那颗按钮接的是真端点。原始记录留着,因为它写明了当时那个**诚实处理**的形状:
  方案 §4.2 的状态机画了 pending 的否决支(删除/标为误报),S4 的七个端点里没有;
  前端没有伪造一个会 404 的调用,而是把「否决」做成仅从本次清单划掉,并在面板底部写明
  那条隐患**仍以 pending 留在库里**,而 pending 不算整改率、不进超期清单、不会被升级。
  处理是诚实的,缺口是真的。
- **两个 api 模块都超 800 行:** `supervision_api.py` 1139(代码行 867)、
  `checkin_api.py` 1131(859)。W9 已经切过一刀(文书正文 → `agents/supervision/documents.py`,
  1357→1139,docx 字节等价验过 540 条指纹)。**再切要动的是那七个 `_work_*`,
  拆完剩一个壳且四道闸要两边互相 import** —— 不是机械重构了。这是个仓库级的模式
  (两个模块现在基本持平),该定的是「api 模块到底允许多大」,不是扭曲某一个文件。
- **`supervision.tsx` 1003 行超 800**(仓内先例:`checkin.tsx` 1046、`ProjectUploadPanel.tsx` 1047)。
  天然接缝是 `SupervisionDocCards` 单独成件,但那会让覆盖件计数从 12+7 变 12+8,
  而计数是同源清单里的一项,值得一起动而不是顺手动。
- ~~**`_STATUS_ZH`(隐患状态八档中文名)现在有两份逐字拷贝**~~ ✅ **2026-08-16 W10·S1 已收敛**
  到 `agents/supervision/scoping.py` 的 `STATUS_ZH`(零依赖叶子模块,两边都能 import
  而不成环)。当年那句「暂时收敛不掉」的理由 —— 「让 supervision_api 再 import tools 就成环」
  —— **只对 tools.py 成立**,对一个叶子模块不成立。这是本条留档的价值:
  「收敛不掉」的结论往往绑在一个具体实现上,换个落点就没了。
- **CLAUDE.md 的同源表进不了 git。** 它被 `/CLAUDE.md` 忽略(文件自己头一句就写着)。
  W9 往里加了 4 行、改了 3 行,又补了四处旧数字(前端覆盖件 5→7 件、vitest 68→163 条、
  被测物一个 lib→两个、`make eval SUITE=routing` 22→33 条)—— **这些队友一条都拿不到**。
  这是 W6「队友接入」那次就存在的洞,W9 只是又往里塞了一批。
- **`PRAGMA foreign_keys` 的位置约束没有进同源表。** S8 实测确认:挪进 `with conn:` 里
  **照样生效**(Python 的 sqlite3 不为 `with conn:` 发 BEGIN),但在**真开着的事务**里设
  就是回读 0、静默失效。也就是说**没有任何行为断言能区分对错位置**。现在靠的是
  `core/sqlite_util.open_db` 把它做成进场参数(调用方物理上设不了),外加注释。
  值得在同源表里单列一行,提醒别把它改回"调用方自己设"。
- **真机验收没跑。** `live_acceptance.py` 的 A3/C3 两句改了措辞(判据一字未动),
  改对没改对只有真机跑一遍才知道 —— 它是演示前唯一的兜底。
- **Depends:** 前四条互相独立;评测重跑与真机验收建议同一天做完。

## TODO-43 🔴 向量库缺 `scope` 元数据 → knowledge 整库不可用,而 `make build-knowledge` 修不了
2026-08-16 W9 真机验收 E2 红,查下来的。**与 W9 无关**(那条分支一个 knowledge 文件都没碰),
但它比 W9 的任何一条都严重:**knowledge Agent 在这台机器上是彻底哑的,而所有表面指标都正常。**

- **现象:** `search_regulation` 恒返回 `EMPTY_RESULT`,detail 写「最近距离 **无命中** > 阈值 0.85」。
  注意是「无命中」不是一个距离数 —— Chroma 一条都没返回,不是返回了但太远。
- **根因:** 库里 872 个 chunk 的 metadata 只有 `page` / `source`,**没有 `scope`**。
  而 `tools._scope_filter()` 无项目上下文时过滤 `{"scope": "global"}` —— 一条都匹配不上。
  这个库是在**作用域功能落地之前**建的(那次给每个 chunk 加了 scope/project_id/doc_type)。
- **实证(容器里跑的):**
  ```
  不带过滤   距离 0.540  GB-50016 p124「消防车道…净宽度和净空高度均不应小于 4.0m」
  带 scope   0 条
  ```
  **答案在库里、距离 0.540 远在阈值 0.85 之内、就是取不出来。**
- **🔴 为什么 `make build-knowledge` 修不了:** `ingest.build_index()` 的 manifest 记
  `{文件名: sha256}`,文件没改过就**跳过整份**并打印「跳过(已入库、未改动)」——
  那句话看着像成功。只有 `--rebuild` 会无视 manifest 重嵌:
  ```
  docker exec gyt-backend python -m gyt.agents.knowledge.ingest --rebuild
  ```
- **🔴 为什么验收抓不到:** E3 断的是「向量库真有 chunk(872)」——**数得出条数,数不出
  metadata 缺字段**。库彻底不可用时 E3 照样绿。E1(路由到 knowledge)也照样绿。
  只有 E2(真要答案)会红,而它红了很容易被归给「模型没答对」。
- **怎么补这条闸(已核过表结构,可直接照抄):**
  ```sql
  SELECT count(*) FROM embedding_metadata WHERE key='scope'
  ```
  和 `count_chunks()` 同一套技术 —— 只读 URI 连 `chroma/chroma.sqlite3`,零 ML 依赖。
  实测现状:`embedding_metadata` 只有三个 key(`chroma:document` / `page` / `source`,
  各 872 行),`scope` 一行都没有;重建之后该是 872。判据写成
  「带 scope 的行数 == chunk 总数」比「> 0」更严(防只重建了一部分)。

  ⚠️ **我一开始在这条 TODO 里写的建议是错的**,留着当反面记录:原话是「拿一个必然
  命中的 query 走一遍**真检索**」。做不到 —— 验收脚本跑在**宿主**上,而宿主装不了
  torch(Intel Mac,`pyproject.toml` 第 171 行写着),`count_chunks` 当初写成直接读
  sqlite **正是为了绕开这件事**。照那条建议去改,会写出一条在本机永远 import 失败的断言。
  教训:**给一个"加断言"的建议之前,先确认那条断言跑得起来。**
- **谁会中招:** 任何一台向量库建于作用域功能之前的机器。因为 manifest 会一直跳过,
  这个状态**不会自愈**,而且每次 `make build-knowledge` 都会告诉你「已是最新」。

- **✅ 2026-08-16 已在本机修复,但修复链是三步,少一步都不行**(实测):
  1. `docker exec gyt-backend python -m gyt.agents.knowledge.ingest --rebuild`
     —— 二十多分钟(CPU 上嵌 872 个 chunk),**输出全程缓冲**,中途查会看到
     「还是 872、还是没 scope」,别以为没生效。
  2. 🔴 **重启服务。** 不重启的话检索报
     `chromadb.errors.InternalError: Error executing plan: Internal error: Error finding id`
     —— 服务进程握的是重建**之前**的 chroma 句柄(`get_vectorstore()` 带 `lru_cache`),
     段文件被另一个进程重写后,缓存句柄指向的 id 已经不存在。
     **这条在生产上更要命**:线上重建规范库不重启,knowledge 会从「查得到」变成
     「报内部错误」,而且不会自愈;而报错内容跟「查不到」完全是两回事,排查方向也不同。
  3. 验:直接查向量库通了**不等于**服务里通了 —— 这次就是直接查 3 条命中、
     通过服务问却报错。两处都要验。

- **🔴 `--rebuild` 清不掉前 scope 时代的残留(同一个洞的第二半,本次实测)。**
  重建后是 **1744 = 872 新 + 872 旧**,不是 872。原因:`delete_document()` 的
  「先删同 source 的旧块」用的是
  ```python
  where={"$and": [{"source": …}, {"scope": …}, {"project_id": …}]}
  ```
  而旧块**没有** `scope` / `project_id` 两个键 → 匹配 0 条 → 删不掉;
  又因为 `_chunk_ids()` 生成的 id 带 `<scope>:<project_id>/` 前缀、与旧 id 形状不同,
  upsert 也覆盖不了,只能并排新增。第二次重建净值不变(删掉新的 872 再重加)即为佐证。
  **不影响正确性**(检索带 scope 过滤,查不到那批),但库体积翻倍,
  而且下一个人看 `count_chunks` 的 1744 会以为有那么多份内容。
  要清干净只能手工删 collection 或按「没有 scope 键」反查 id 后删。
- **Depends:** 无。

## TODO-44 C3 断言判在全角/半角括号上 —— 事实全对却红
2026-08-16 W9 真机验收第二轮 C3 红,而**同一份代码第一轮是绿的**。

- **判据:** `D.c3_display in whole`(纯子串)。`c3_display` 由 `dates.py` 生成 `8月19日(周三)`,
  **半角括号** U+0028/U+0029。
- **模型实际写的:** `8月19日（周三）`,**全角** U+FF08/U+FF09。差一个码点,断言就红。
- **事实层面全对:** 路由 `transfer_to_schedule` ✓、日期 8月19日 = 下周三 ✓、
  **D3 库级断言 PASS**(整改任务真实落库、期限 2026-08-19)。
- **两轮不同的原因:** 上下文不一样(照片编号、任务标题都不同),模型这次挑了全角。
  temperature=0 只保证同输入同输出,不保证跨轮标点一致。
- **这正是 TODO-25 记的那件事**(「验收大半在断措辞,不是断事实」)的一个具体实例。
- **改法(择一):** ① 比对前把全角括号归一成半角(只归一括号,别动别的字符);
  ② 判据从「显示串子串」换成「日期数字 + 星期字」两个更稳的锚。
  ⚠️ **别放宽成只断日期数字** —— `due_display` 一个字不改地复述是 prompt.md 的铁律三,
  彻底不管它等于把那条铁律的唯一验证点删掉了。
- **Depends:** 无。改完建议连跑两轮确认不再抖。

## TODO-45 W9 代码评审(2026-08-16 第二轮,审实现不审方案)剩下的 8 条
方案审过三轮,**代码没审过**。这轮 `/plan-eng-review` 我找 4 条 + Codex 找 9 条 = 13 条,
其中 5 条已核实的当场修了(见 PR #27 的后续提交)。以下 8 条记账。

**这 13 条的共同点值得先说:** 六个 agent 并行实现,每人把自己那份写到 98-100% 行覆盖,
而**没有任何一条测试跨过文件边界**。13 条里没有一条是行覆盖能抓的。
这是并行实现的固有失效模式,不是谁偷懒 —— 下次并行开工前就该先约定「谁负责接缝」。

### A. 跨实现漂移(我找的三条,判据都重复了 2-3 份)
- **处置判据两份实现,无同源测试。** `agents/supervision/tools.py:503` 的 `_advise` 与
  `supervision_api.py:480` 的四道闸各写一遍;`test_supervision_tools.py` 零 import api。
  端点是权威且守住了,所以这是**体验漂移不是安全漏洞** —— 但后端改了闸、Agent 还按旧规则
  发话,监理照着做一直被拒,而两边测试都绿。
- ~~**`_STATUS_ZH` 三份拷贝**~~ —— **Python 侧两份已于 2026-08-16(W10·S1/S2)收敛**,
  唯一真相在 `agents/supervision/scoping.py` 的 `STATUS_ZH`。收敛的前提是 scoping.py
  是**零依赖叶子模块**,`supervision_api` 与 `tools` 都能 import 它而不成环 ——
  老注释里那句「让 supervision_api 再 import tools 就成了环」只对 tools.py 成立。
  `test_supervision_api.py` 有一条 `not hasattr(supervision_api, "_STATUS_ZH")` 钉着**别抄回来**。
  **剩下的跨语言那一份仍在**(`supervision-lib.ts` 的 `HAZARD_STATUS_ZH`),它收敛不掉,
  仍归 A 组的同源向量方案。
  ⚠️ 顺带补一句当时没写清的:导入期守卫**只数键、不比值**,所以「已出具暂停令」被改成
  「已责令停工」它一声不吭 —— `test_supervision_scoping.py` 现在有一条专门盯这句话
  (W10·S1 的变异测试②b 证明了守卫对这种漂移全程沉默)。
- **前端 `availableActions` 是闸门的第三份实现**(`supervision-lib.ts:440/446`)。
  🔴 **方向不对称**:多显一个按钮 → 点了被拒,用户看得见;**少显**一个按钮 → 合法动作
  做不了,**一句报错都没有**。后者是本仓反复吃亏的那类静默失败。
- **三条的定案都是 A:加跨实现/跨语言同源向量**,照 `checkin-lib.test.ts` ↔
  `test_checkin_api.py` 的 `ROUNDTRIP_VECTORS` 先例(两边注释互指、逐条一致)。
  穷举 32 种组合(八态 × 两级别 × needs_grading 两值)的向量写一遍,
  后端 pytest 与前端 vitest 各跑各的。

### B. Codex 找到但本轮未核实的四条(核实前别当定论)
- **[高] `supervision_api.py:986`** 升级报告的证据快照与状态迁移不在同一事务,
  而 `reinspect_failed` 允许自环 → 并发新增一次复查仍不改状态,升级照样成功但
  「完整证据链」漏掉最新那条记录。
- ~~**[中] `supervision.tsx:422`** 「否决」只删本地数组~~ —— **2026-08-16(W10)已修**:
  加了 `POST /supervision/reject`,前端那颗按钮接的是真端点(成功后 `removeHazard`)。
  容器里实测过全套:pending 否决 → 200 且直查 sqlite 确认真删了;
  已确认的 → 409 且直查确认**那条还在**。`delete_pending()` 不再是零调用。
  (S6 当时如实写了「后端没有否决端点」、没伪造一个会 404 的调用 —— 处理是对的,缺口是真的。)
- **[中] `agents/safety/tools.py:657`** 编号预查与 `hazards.create()` 之间撞唯一键时
  直接进 `failed_items`,没按 `core/doc_no.generate_unique` 的契约重摇号 → 并发撞号会**漏登记一条真实隐患**。
- **[低] `supervision_api.py:755`** 单条确认不存在的隐患返 `409/CONFLICT`,
  与模块 docstring 声明的 `404/NOT_FOUND` 契约不符 → 调用方会把「编号不存在」误判成「状态并发」。

### C. 文档与现实的落差
- **「整改率」在 5 个文件的注释里被反复承诺,而没有任何代码算它**
  (`safety/tools.py:54,146` / `supervision/tools.py:153,494` / `db/hazards.py:68`)。
  F4 汇总被 D1 砍了,所以这是对**将来某人**的约束,而约束只活在散落的散文里。
  F4 落地时必须回来看这五处,或者先把「不计入统计的状态集合」做成一个具名常量
  (`_OVERDUE_STATUSES` 已经是超期那一侧的先例)。

**Depends:** A 组三条共用同一份向量,建议一起做;B 组四条互相独立,先核实再动手。

## TODO-46(原始记录)本机容器**从来没服务过任何一条自定义 HTTP 路由**
2026-08-16 W10 的 S4 手工验之前发现的。**已修**,但形状值得单独留档 ——
它和 W10 本身是同一类洞,而且是在查另一个洞的路上撞见的。

**怎么发现的:** 手工验之前先 curl 一下已有的端点当基线,期望 400(空 body):
```
curl -X POST http://127.0.0.1:2024/supervision/confirm -d '{}'   → HTTP 404
docker exec gyt-backend ls /app/webapp.py                        → No such file or directory
docker exec gyt-backend cat /app/langgraph.json                  → **连 http 块都没有**
```
镜像 `gyt-backend:dev` 是 5 天前(≈08-11)烤的,而 `webapp.py` 与 `langgraph.json` 的
`http` 块是 **W7(08-15)** 才加的。也就是说:**打卡、项目管理、监理处置,本机一条都没跑起来过。**
在这之前所有"本地验过"的结论,都只覆盖了聊天那条路。

**根因不是文档写错。** `docker-compose.dev.yml` 那段「为什么挂 /app/src 就够了」写得很准,
它列的重建触发项里就有 `langgraph.json`。错在**没人意识到 `webapp.py` 与 `auth.py`
也在 `src/` 外面** —— 它们是 `.py`,看着就该跟着热重载,于是没人去想「这一条要重建镜像吗」。

**修法**(已落):把这三份也挂进 dev 覆盖件。`webapp.py` / `auth.py` 是 `.py`,
跟着 watchfiles 热重载;`langgraph.json` 不是 `.py`,改它仍要 `restart`,
但**不用再重建镜像**(分钟级 → 秒级)。生产档不许照抄这三条(线上必须跑烤进镜像那份)。

**为什么留档:** 这个洞的形状 = **后端代码是对的、测试全绿、端点 404 而零报错**。
与 W10 完全同构。`test_supervision_api.py` 里那条路由挂载用例**当时也是绿的** ——
它在宿主的 Python 进程里直接 import,根本不经过容器。那条用例的 docstring 里现在写了这一段。

## TODO-47 W10 落地后的残余欠账
四条泳道落地。以下是**明知留下**的,每条都写了为什么现在不做。

- **`unassigned` 与当前 scope 口径错位。** 后端给的 `unassigned` 是「未归属**一共**几条」,
  **不过 scope 筛子**(刻意的:它回答「有没有一批隐患没人看得见」,筛过反而藏起来)。
  但界面只能在一屏之内说它 —— 筛「超期」时也会显示「另外还有 N 条没归到任何工地」,
  而那 N 条里可能一条超期的都没有。措辞上已经回避(只说「没归到任何工地」),
  语义上仍然错位。要真准,后端得另给一个「当前 scope 下未归属几条」。
- **🔴 `failedItems`(D10 / Codex#10)现在没有任何地方在履行「必须显示」。**
  它挂在 `HazardIntakeCard` 上,而**整张卡不可达**(W10 §1 的根因)。也就是说:
  一条真实存在、后端已经落了 `hazard_ingest_failures` 一行的隐患,
  **工友在界面上一个字都看不到**,而识别回执照常报了这一项。
  不是 W10 引入的(卡本来就没显示过),但 W10 之后它是**已知**的洞了。
  修法多半是把它并进常驻操作台(那里已经有数据源了),不是去救那张卡。
- **巡检记录卡(W3/W5)仍不可达。** 同一根因,W10 §4 明确不在范围:
  它牵扯 `report` 与 `hazards` 的关系(docx 在 `artifacts` 里,不在 `hazard_docs` 里,
  现有详情接口捞不到),该单独想清楚。**在它修好之前,整条 `ARTIFACT_BASE` 同源链
  只有监理这一半是活的。**
- **四个前端覆盖件超 800 行**:`supervision.tsx` 1402、`supervision-lib.ts` ~1230、
  `checkin.tsx` 1046、`ProjectUploadPanel.tsx` 1047、`checkin-lib.ts` 914。
  **一起拆,别只拆一个** —— 只拆一个是 churn 而不是原则;而且拆错的失败模式
  (`setup-frontend.sh` 漏注册 → 界面静默不生效)正是 W10 这个洞本身。
  拆的时候两个计数(`apply_override` / `install_new_file`)必须一起改,它们过期过两次了。
- **`scoping.py` 的「零 langchain」只在文件级成立。** `import gyt.agents.supervision.scoping`
  照样会拉起 langchain —— Python 先执行父包 `__init__.py`,而那里 import 了
  `tools.SUPERVISION_TOOLS`。这笔账是既成事实(`supervision_api` 早就 import `documents`),
  不是搬迁带来的。真要摘干净得把包 `__init__.py` 里建 Agent 那段延迟到函数里
  (`graph.py:147` 从包名 import `build_supervision_agent`)。
  兑现之后 scoping 才真的能被一个不想要 langchain 的调用方用上。
- **CLAUDE.md 的同源清单又欠了几行,而它进不了 git**(`/CLAUDE.md` 被忽略)。
  W10 要加/改的:`ARTIFACT_BASE` 读者三 → **四**(加 `supervision-entry.tsx`)、
  scope 受控词表跨语言镜像(`scoping.SCOPES` ↔ `supervision-lib.HAZARD_SCOPES`)、
  隐患状态中文名那一行(两份拷贝 → 收敛到 `scoping.STATUS_ZH`,前端那份仍是镜像)、
  前端覆盖件计数(`install_new_file` 七 → **八**)。**队友一条都拿不到** ——
  这是 W6「队友接入」那次就存在的洞,W10 只是又往里塞了一批。

**Depends:** 前三条互相独立;拆文件那条建议和 CLAUDE.md 计数一起做(它们本来就同源)。

## TODO-48(原始记录)真人试用抓到的两条 —— 都不是实现写错,是设计时没想到工地现场
2026-08-16 W10 落地当晚,真人点界面反馈的两条。**单元测试、类型检查、端到端脚本
全绿的时候它们都在** —— 因为它们不是「代码没按设计跑」,是**设计本身没站在工地上想**。

### ① 「这个系统锁死的不明显」→ 查下去发现锁错了一颗(已修,`a4b54f3`)
原话是在说观感:当前级别那颗按钮 disabled,但「严重」恒用 destructive(实心红)——
而面板里实心红是**主操作**的样子,一颗看着是主操作、点下去没反应的按钮就是「坏了」。

顺着查发现**功能也错**:判据是 `hazard.grade === grade`,而 `hazard.grade` **永远有值**
—— `needs_grading=1` 时那是映射表给的默认档(一般),不是结论。于是**待定级的隐患
「定级为一般」那颗被禁用**:监理认定它就是一般隐患却点不下去,唯一能点的是「严重」。
而硬拦②防的正是「一般隐患签了暂停令 = 平白停一片人的工」。

判据已换成 `currentGrade()`(认 `needs_grading` 旗子,与后端 `_advise` /
`_require_graded` 同源),下沉到 lib 并有变异验证。

**留档的价值不在这个 bug,在它的形状**:「默认档冒充结论」这一类,
仓里已经在三处分别防过(`_GRADE_UNKNOWN` / `_hazard_line` / `_advise` 的判据顺序),
而界面这一处**没人想到它也算一处**。下次再出现「某字段永远有值,但它有没有意义
取决于另一个旗子」的形状,先去数一遍有几个消费者。

### ② 🔴「照片不能是编号意义不明」—— 复查要填 32 位 hex
「登记复查结论」要求填 `after_photo_id`。做复查的人手机里刚拍完那张照片,
却要他去聊天记录里翻到那张图、把图下面那串十六进制抄过来。**工地上不成立。**

而这不是偷懒:**后端当时没有任何通用的传图口子** —— PHOTO 产物只能经由聊天的
`core/uploads.py` 产生(`grep -rn "ArtifactKind.PHOTO" backend/src/gyt/`:
一处在产、一处在校验)。所以「抄编号」是当时唯一能走的路。

修法(进行中):补 `POST /supervision/photo` 收裸图片字节(照 W7 打卡那条成熟路子,
按**魔数**判类型而不是信 Content-Type —— 那张照片是销项的唯一证据,
放个 .txt 进去等于拿文本文件销项),前端换成 `capture="environment"` 直接开后置摄像头 +
缩略图预览,编号那格降级成折叠的备选(有人确实要引用聊天里已传过的图)。

**这一条同样值得记形状**:凡是界面上要人**手抄一个系统内部标识符**的地方,
都是同一个洞。现在还剩几处没数过。

**Depends:** ① 已修;② 两条泳道进行中(端点 + 界面)。

### ③ 照片直传端点带出来的三笔新账(端点本身已落,`43f161d`)

- **🔴 上传口不经过任何状态机,而 supervision 整摊「不设限流桶」的理由正是
  「状态机本身就是闸」**(模块头注原话:「同一条隐患签过一次,第二次会被迁移守卫拒掉,
  刷不出量来」)。那条理由现在**缺了一角**:一个已登录的人循环传 10MB,
  每次都真落一份产物,量只由 Caddy 的 body 闸和磁盘决定。
  该加的是按**字节**计的全局桶(不是按次数),`checkin_api.get_global_limiter` 有同型先例。
- **孤儿照片没有清理器。** 传上来但从没被 `reinspect-result` 引用的照片会永远躺在盘上
  —— `attendance/cleanup.py` 只删 ATTENDANCE,PHOTO 不归它管。这是方案 §8 已认下的
  那笔账在新入口上又开了一个口子,而**这个口子比签发失败留孤儿文书的频率高得多**
  (选错文件、传了不点提交,都会留)。
- **`_require_photo` 认任何 PHOTO 产物,包括聊天链上传的那些。** 也就是说
  「首次发现那张照片」的编号能被当成「整改后」的证据回填 —— 这条端点没让它更糟,
  但也没修。真要堵,得给复查照片单列一个 kind(或在 sidecar 上记来源),那是另一件事。
  ⚠️ 这一条与「复查必须挂照片」那条红线是同一件事的两半:红线管的是「有没有照片」,
  管不了「是不是整改后那张」。

### ④ 一条关于**测试怎么会骗自己**的记录(变异测试暴露的)
给照片端点写「非图片一律拒」时,用例是 docx / 纯文本 / WAV 三条。
把实现变异成「只信 Content-Type、不判魔数」之后,**那三条一条都没红** ——
因为 httpx 用 `content=bytes` 时**不带 Content-Type**,于是「只信 header」的实现
照样拒了它们。真正抓到缺陷的只有另写的那条「Content-Type 说是图片也不算数」。

**形状:测试用例里那个"不该被信任的输入"根本没被送进去。** 断言在验一条压根没走到的路。
下次写「不许信任 X」的用例,先确认 X 真的**出现在请求里**。

---

## TODO-49 W12 第二批(英文答话 + 界面三语)—— 负责人 2026-08-18 决定先不做

- **What:** 三语切换的后一半。繁體答话已上线(`feat/w12-lang-hant`,10 个提交),
  **英文答话与界面 166 条文案没做**。原话:「做到繁体就结束,英文的先不做」。
  完整方案在 `docs/W12_三语切换_方案.md`(md + html 双件套),§5 那张表里
  标着 🛑 的就是这批。

- **Why 两件必须一起做:** 繁體用户**读得懂**简体按钮(简繁互通度高),所以
  「答话繁體 + 界面简体」这个落差可以接受 —— 这正是第一批只做答话的依据。
  **英文用户读不懂简体按钮**,所以英文答话单独上线没有意义:
  他连「发送」都按不下去。

- **要做的时候动这些:**
  ```
  core/base_agent.py   加 context_schema(一行,八个 Agent 全过它)
  core/speak_lang.py   新中间件 —— **只在 en 时注入**,其余语种原样返回 request
  graph.py             supervisor prompt 改 callable(实测 Prompt 收 callable)
  core/run_context.py  gyt_lang 键(复用 gyt_project_id 那条已通的管道)
  thread-index.tsx     两处 submit 带上 lang
  前端 i18n/           工友表面 166 条(已数清)+ 动作条上的切换器
  ```

- **🔴 三条不许忘的:**
  1. **中间件只在 `en` 时注入**。其余语种必须**原样返回 request**(连
     `override()` 都不调)—— 否则 1102 条文本缓存当场全废,而且零报错,
     表现只是「每次提问都变慢、都真花钱」。
  2. **注入的英文指令不能写成「不许出现汉字」。** 那会和 §4.7 定案(knowledge
     的出处行与拒答句保中文)**直接冲突**,模型会随机解一个。必须显式开例外。
  3. **改了 supervisor 提示词 = routing 那个 33/33 留档数当场失效,必须重跑。**
     判据是「不低于门槛 90%」,不是「必须还是 33/33」—— 掉一两行完全可能只是
     提示词敏感度(轮间噪声实测有 25 个百分点,见方案 §4.5.4)。

- **已经先做掉的一件(不用重做):** `knowledge/prompt.md` 的出处行与拒答句
  保中文 + 同源守卫(提交 `c05d8e4`)。它本是英文那批的前置约束,但它同时挡着
  一个**静默放行编造**的洞,所以提前落了。
  ⚠️ **副作用:那条提示词规则现在是待命状态** —— 英文没上线,模型收不到英文请求,
  它一次都不会触发。留着不删,英文上线即生效,守卫已经在钉着它。

- **Depends:** 无硬依赖。**曾经以为卡 W11 术语定案,那是记错了** ——
  卡 W11 的只有「监理面板 417 行 + 六种文书标题 + 术语白名单」那一组(方案 §7)。

- **建议顺序:** 动这批之前先收 **TODO-9**(Agent 路径的英文 traceback 中文化)。
  理由是判据:现在「界面上出现英文 = 出事了」还成立;英文答话一上线,
  英文报错会混在英文界面里,**再也认不出是 bug 还是正常输出**。
