# =============================================================================
# 工友通(GYT)VPS 公网部署环境变量样例
#
# 用法(在 VPS 的仓库根目录):
#     cp .env.vps.example .env
# 然后按下面每一节的说明把值填上。
#
# ⚠️ 文件名必须是 **.env**,不要留成 .env.vps 然后用 --env-file 指过去。
#    原因:--env-file 只影响 docker compose 自己的变量插值,
#    **不影响** docker-compose.yml 里 backend 那条 `env_file: .env`。
#    那条是 required:false —— 文件不存在时它不报错,后端就这么在
#    「一把模型 Key 都没有」的状态下启动,然后在建图时挂掉。
#    现象是容器反复重启,而日志里完全看不出「你的变量放错文件了」。
#
# ⚠️ 这个文件里**只有示例值**,一个真实密钥都没有。
#    填好的 .env 绝对不要提交、不要发群里、不要截图。
#
# 本文件和仓库根的 .env.example(本机开发用)是两份东西:
#   .env.example      本机开发,只要两把模型 Key 就能跑
#   .env.vps.example  公网部署,多了鉴权、地址、时区这几组
# 公网部署时以本文件为准 —— 它是前者的超集。
# =============================================================================


# =============================================================================
# 零、要部署哪个版本(必填,2026-08-23 起)
# =============================================================================

# 后端镜像的 tag = 要部署的那个 **commit SHA**(40 位全长)。
#
# 镜像由 CI 构建并推 GHCR(见 .github/workflows/ci.yml 的 build-push job),
# **VPS 不再自己构建** —— 理由是 TODO-40:这台机器 30GB 盘装不下一次全量重建,
# 而失败会把 tag 打到半截镜像上、旧镜像记录被 containerd GC 掉,
# 最后落到「容器还活着但既不能打 tag 也不能重启」这种外面看不出来的状态。
#
# 值从哪儿来:合进 main 之后,那次 CI 运行的**摘要页**会直接给出完整部署命令。
# 也可以 `git rev-parse HEAD`(前提是本地 main 与线上要部署的版本一致)。
#
# ⚠️ **别填浮动 tag(latest / vps 之类)。** 浮动 tag 的坏法是「以为部署了、
#    其实跑的还是旧镜像」,而 `up -d` 不会自动重拉。本仓已经因为「线上到底是
#    哪个提交」这件事含糊,拿一个 12 天前的旧数做过一次错误的风险评估(TODO-33)。
#
# ⚠️ 拉之前先量磁盘:`df -m /` 的可用要 ≥ 压缩下载量 + 解开后大小。
#    2026-08-23 实测:压缩 1855MB、解开 6.3GB,峰值约 8.2GB。
#    **新容器 healthy 之后才删旧镜像** —— 顺序反了就没有回滚点了。
GYT_IMAGE_TAG=

# =============================================================================
# 一、两把模型 Key(必填,和本机开发完全一样)
# =============================================================================

# DeepSeek 密钥。申请:https://platform.deepseek.com/api_keys
# 供路由 / 任务拆解 / 报告生成 / 知识综合使用(文本模型 deepseek-v4-flash)。
# ⚠️ 留空,不给占位符 —— 理由同 GYT_ACCESS_TOKEN。
#    core/llm.py:312 的判据是 `if not provider.api_key`,**只认空**:
#    填了占位符 → 非空 → 建图那关照样过 → 容器正常起来 → 然后**每一次对话**
#    都在第一次模型调用时报错。留空则建图期就抛 MissingAPIKeyError,容器起不来,当场就知道。
GYT_DEEPSEEK_API_KEY=

# Moonshot(Kimi)密钥。申请:https://platform.moonshot.ai/console/api-keys
# 供 Safety 识图和 CAD 预览问答使用(视觉模型 kimi-k3)。
# 注意是国际站 .ai,不是 .cn,两边的 key 不通用。
GYT_MOONSHOT_API_KEY=


# =============================================================================
# 二、鉴权(必填,少一条就别上公网)
# =============================================================================
#
# 两道门,防的是**不同的失效模式**,不是重复劳动:
#
#   ① GYT_LOGIN_HASH —— 登录页校验的口令哈希,给人的门。
#      外部测试的人只需要知道这一个口令,别的什么都不用配。
#      (2026-08-11 之前这里是 GYT_BASIC_AUTH_HASH + 浏览器原生弹框。
#       换成登录页的完整理由见 scripts/serve_login.py 顶部。)
#
#   ② GYT_ACCESS_TOKEN —— 后端校验的 X-Api-Key,纵深第二道。
#      防的是「Caddy 这层失效」:路由写错、有人排障时给 backend 加回 ports、
#      或者这台机器上多起了一个容器直连内网。①塌了②还在。
#      它会被编译进前端 JS 包,所以对**终端用户不是秘密** —— 这是刻意的取舍:
#      能拿到这个包的人已经过了①那道门,而换来的是测试的人不用手动粘令牌。
#
# -----------------------------------------------------------------------------

# --- ② 后端令牌 -------------------------------------------------------------
#
# 生成命令(在任意一台有 openssl 的机器上跑,VPS 上就有):
#
#     openssl rand -hex 32
#
# 为什么用 hex 而不是 base64:hex 只有 0-9a-f,
# 不会出现 `$` `+` `/` `=` 这些在 .env / shell / URL 里需要额外操心的字符。
# 32 字节 = 64 个字符 = 256 位熵,猜是猜不出来的。
#
# 生成完把整串粘到 = 后面,不要加引号。
#
# ⚠️ 这里**刻意留空**,不给占位符。2026-08-11 安全复核实测:占位符是非空字符串,
#    能顺利通过 docker-compose 的 ${GYT_ACCESS_TOKEN:?…} 检查 —— 于是「照抄模板没改」
#    的部署会一路绿灯上线,而令牌是一串公开写在 git 仓库里的字。
#    留空则 `docker compose up` 当场失败并说人话,这才是真的 fail-closed。
#    (后端 auth.py 还有第二道:占位符 / 短于 24 位一律当没设,启动打感叹号横幅。)
GYT_ACCESS_TOKEN=

# --- ① 登录口令 -------------------------------------------------------------
#
# ⚠️ 没有用户名这一项了。登录页只要口令 —— 内测环境,一个口令发给几个人,
#    再让人记一个 "gyt" 用户名纯属增加摩擦。
#
# 口令的 **scrypt** 哈希。**不能填明文口令**。
# (为什么是 scrypt 不是 bcrypt:hashlib.scrypt 在 Python 标准库里,bcrypt 不在。
#  login 服务跑在 python:3.12-slim 上、零 pip 安装 —— 不为一个哈希函数
#  多背一个包和一条构建期网络依赖。scrypt 还是内存硬的,抗 GPU 爆破更好。)
#
# 一条命令搞定(它会生成口令、算哈希、并且**把 $ 加倍好**):
#
#     python scripts/make_login_hash.py --random     # 自动生成一个 20 位口令
#     python scripts/make_login_hash.py              # 或者自己想一个(不回显)
#
# 把它吐出来的整行粘到下面:
#
#     docker run --rm caddy:2-alpine caddy hash-password \
#       --algorithm bcrypt --bcrypt-cost 12 --plaintext '你刚想好的口令' \
#       | sed 's/\$/$$/g'
#
# ⚠️⚠️ 末尾那个 `sed 's/\$/$$/g'` **不能省**,这是本文件最容易踩的坑。
#
#   bcrypt 哈希长这样:$2a$12$xxxxxxxx
#   而 Docker Compose 会把 .env 里的 `$` 当成变量引用吃掉。实测(Compose v5.1.3):
#       .env 里写   $2a$14$V8/gFXj/yURxTSO0eV37Te7fHfXQ4kwizuVBbvVQskVnVL3iqwD2S
#       容器里拿到  $2a$14.ZwGx8MTWE.vgqIl753P4bT/MUvMgFfK        ← 中间少了一截
#   最坑的是 Caddy 对残缺哈希**不报错**,照常启动,只是口令永远对不上:
#   你输的明明是对的口令,页面一直 401,而你会去怀疑自己是不是记错了。
#   sed 那一下就是把每个 `$` 写成 `$$`,Compose 还原后正好是原始哈希。
#
#   自查方法:下面这行里的 `$` 应该**全是成对的**($$)。只要看到单个 $,就是错的。
#
# ⚠️ --bcrypt-cost 12 也别省。不写的话 caddy 默认用 cost 14,
#   实测单次校验要 1.25 秒 CPU(cost 12 是 315ms,cost 10 是 84ms)。
#   basic_auth 排在所有路由之前,任何陌生请求都会触发一次 ——
#   在 2C4G 的机器上,两个并发的乱试就能把 CPU 打满。
#   完整取舍记录在 Caddyfile 里 route 那一段和 scripts/serve_login.py 顶部。
#
# ⚠️ 同样**刻意留空**。这一项填错的失败模式更阴:哈希是假的时候 login 服务会
#    **照常启动**,只是所有口令都对不上 —— 人会以为自己口令记错了,
#    去查口令、查大小写、重生成哈希,而真因是模板压根没改。留空则起不来,当场就知道。
GYT_LOGIN_HASH=


# =============================================================================
# 三、公网地址(必填两个,取值互相对应)
# =============================================================================
#
# 这两个变量必须描述**同一个入口**,填不一致的典型现象是:
# 页面打得开,但一发消息就转圈或报连不上(因为前端被编译成去连另一个地址了)。
#
# 场景 A:没有域名,直接用 VPS 的 IP(当天最快上线的走法)
#     GYT_SITE_ADDRESS=:80
#     GYT_PUBLIC_ORIGIN=http://203.0.113.10          ← 换成你 VPS 的公网 IP
#   ⚠️ 纯 HTTP,basic_auth 的口令是明文过网的。临时测试可以,
#      但别用你在别处用过的密码,也别把它当长期方案。
#
# 场景 B:有域名(推荐)。先把 A 记录指到 VPS 的 IP,等生效了再启动。
#     GYT_SITE_ADDRESS=gyt.example.com
#     GYT_PUBLIC_ORIGIN=https://gyt.example.com      ← 注意是 https
#   Caddy 会自动申请 Let's Encrypt 证书、自动续期、自动把 80 跳到 443。
#   前提是 80 和 443 都能从公网进来(ACME 的 HTTP-01 校验走 80)。
#
# ⚠️ 改完这两个必须**重建前端镜像**,只 restart 不管用:
#    NEXT_PUBLIC_API_URL 是 Next.js 的编译期变量,烘在浏览器包里。
#        docker compose -f docker-compose.yml -f docker-compose.vps.yml up -d --build frontend
# -----------------------------------------------------------------------------

# Caddy 监听什么。裸 IP 填 :80(前面那个冒号别丢);有域名就填域名本身。
GYT_SITE_ADDRESS=:80

# 测试的人在浏览器地址栏里敲的东西,**不要带结尾斜杠**。
# compose 会在它后面拼 /api 作为前端连后端的地址。
GYT_PUBLIC_ORIGIN=http://203.0.113.10


# =============================================================================
# 四、时区(建议填,别用 VPS 的默认值)
# =============================================================================
#
# 绝大多数 VPS 默认是 UTC。而本项目的排期台账是按「今天 / 明天 / 本周五」
# 现算日期的,差 8 小时的直接后果是:北京时间晚上 8 点之后建的任务,
# 日期全部落到前一天。界面不报错,只是日子悄悄不对 ——
# 这种错测试的人根本描述不清楚,只会说「感觉怪怪的」。
#
# 已确认后端镜像自带时区数据(backend/Dockerfile 第 246 行注释:
# python:3.12-slim-bookworm 自带 /usr/share/zoneinfo),设了就生效,不用装 tzdata。
#
# 上线后自查(应该打出北京时间,不是 UTC):
#     docker compose -f docker-compose.yml -f docker-compose.vps.yml exec backend date
TZ=Asia/Shanghai


# =============================================================================
# 五、数据目录(一般不用动)
# =============================================================================
#
# 容器内的数据根目录。台账 sqlite、LLM 缓存、上传件、产物、Chroma 向量库都在这下面。
# 基础档 docker-compose.yml 已经把它设成 /app/data,并把宿主的 ./data 挂了进去,
# 所以**正常情况下这一行不用取消注释**。
#
# ⚠️ 要动的话必须填绝对路径。填相对路径会按进程工作目录解析,
#    而本仓不同启动方式的工作目录并不一致,结果是同一套代码写出两份互不可见的数据
#    (界面上销掉的账,换个方式启动一看还是 open)。完整教训见 .env.example 里的说明。
# GYT_DATA_DIR=/app/data
#
# 宿主机上对应的目录是仓库根的 ./data。Linux 上第一次部署要改属主,
# 否则容器里的非 root 用户写不进去(现象:启动就报权限错):
#     mkdir -p data && sudo chown -R 10001:10001 data


# =============================================================================
# 六、可选:限流与开销控制
# =============================================================================
#
# 下面这些都有代码里的默认值,不填就用默认值。
# **所有常量的唯一入口是 backend/src/gyt/config.py 的 get_settings()** ——
# 想知道某个值现在到底是多少,去那里看,别在别处找第二份真相。
#
# 给外部的人测试时,真正值得盯的是「有没有人在刷 run 烧账单」。
# 后端的令牌桶限流参数(具体变量名以 config.py 为准)在这里覆盖。
#
# Supervisor 熔断步数上限。它限的是**单次 run 内部转手多少次**,
# 不限一个人能发起多少次 run —— 这两件事完全不同,别指望它挡刷量。
# GYT_SUPERVISOR_RECURSION_LIMIT=8
#
# LLM 响应磁盘缓存。公网测试期间建议保持开启(默认 true):
# 多个人问相似问题时能直接命中缓存,省钱也快。
# GYT_LLM_CACHE_ENABLED=true
#
# 上传件大小上限(MB)。注意 Caddy 那一层还有一道 128MB 的请求体上限,
# 是按 GYT_DRAWING_MAX_MB=64 经 base64 膨胀 4/3 算出来的 ——
# 调大下面这个就要回 Caddyfile 把那个数一起改,否则大图纸会被 Caddy 用 413 挡掉,
# 而工友看到的不是那句写好的中文提示。
# GYT_DRAWING_MAX_MB=64.0
# GYT_PHOTO_MAX_MB=10.0
# GYT_DOCUMENT_MAX_MB=10.0
#
# 知识库预建开关。**公网档千万别设成 true** ——
# 首次建库十几分钟,而 backend 健康检查的 start_period 只有 120s,
# 结果是容器永远探不活、caddy 因为 depends_on 也起不来、整站打不开。
# 正确做法是先建好库再起服务,命令见 docker-compose.vps.yml 末尾。
# GYT_KNOWLEDGE_PREBUILD_AT_STARTUP=false


# =============================================================================
# 七、上线前自查清单
# =============================================================================
#
# [ ] 1. GYT_LOGIN_HASH 里的 $ 全是成对的($$),没有落单的,且以 scrypt$$ 开头
# [ ] 2. GYT_ACCESS_TOKEN 是随机生成的,不是 "test" / "123456" 这种
# [ ] 3. GYT_SITE_ADDRESS 和 GYT_PUBLIC_ORIGIN 描述的是同一个入口
# [ ] 4. 两把模型 Key 都填了,且是能用的
# [ ] 5. data 目录属主已经改成 10001:10001(仅 Linux)
# [ ] 6. 配置能解析:
#          docker compose -f docker-compose.yml -f docker-compose.vps.yml config >/dev/null
# [ ] 7. backend / frontend 确实没有 ports(只有 caddy 那三条):
#          docker compose -f docker-compose.yml -f docker-compose.vps.yml config | grep -n -A3 'ports:'
# [ ] 8. 起来之后,**在另一台机器上**验证没有裸奔:
#          curl -sS -o /dev/null -w '%{http_code}\n' http://<VPS_IP>/           → 401
#          curl -sS -o /dev/null -w '%{http_code}\n' http://<VPS_IP>/api/ok     → 401
#          curl -sS -m 5 -o /dev/null -w '%{http_code}\n' http://<VPS_IP>:2024/ok → 连不上
#       第三条必须在别的机器上跑:在 VPS 自己身上跑走的是回环,测不出对外开没开。
# [ ] 9. 用口令能进,且能真正发出一条消息拿到流式回复
# =============================================================================
